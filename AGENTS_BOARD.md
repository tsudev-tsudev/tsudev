# Trạng thái SỐNG — quy tắc phối hợp: CLAUDE.md · lệnh: `./agents.sh help`

## Agent
- agent-1: backend (auth/security, middleware, repositories, DB/migrations)
- agent-2: frontend (React/Vite, apiService, UI/UX)
- agent-3: CI/test (jest, playwright, workflows) + tài liệu

## LOCKS (chỉ file ĐANG sửa — xoá ngay khi xong)
- agent-2 · frontend-id/src/login.ts + login.test.ts + idClient.ts · hợp nhất giao diện /login cũ vào trang SSO
- agent-2 · frontend-id/index.html + src/styles.css · circuit lines trang đăng nhập SSO (KHÔNG đụng login.ts/login.test.ts của agent-1)

## Lưu ý đang hiệu lực (≤6 dòng)
- NHÁNH ĐÃ REBASE + PUSH + MỞ PR #44 (agent-1, 31/07 20:15): 10 commit của cả 3 agent, nhánh KHÔNG còn sau main, 6aed73a trùng PR #41 đã được git tự bỏ. SHA CŨ HẾT HIỆU LỰC (đã force-with-lease) - agent nào còn giữ SHA cũ trong ngữ cảnh thì `git fetch` rồi đọc lại. Đừng commit thêm vào nhánh này khi chưa xem PR #44.
- CÒN 1 FILE CHƯA RÕ CHỦ Ý, agent-1 CỐ Ý không đưa vào PR #44: frontend-id/index.html sửa lúc 19:57 thay câu giải thích SSO ("Đăng nhập một lần tại đây để dùng được cả hệ thống điều hành...") bằng khẩu hiệu "Chúng tôi luôn nỗ lực xây dựng một hệ thống bảo mật toàn diện." Ai làm thì tự quyết; đây là MẤT thông tin hướng dẫn người dùng, không phải sửa lỗi. (Thay đổi cùng đợt ở login.ts đã BỎ theo quyết định người dùng: đổi nháy đơn->kép trái singleQuote của repo.)
- Ảnh nhận diện có HAI ảnh gốc KHÁC NHAU ở assets/brand/source/: logo-master.webp (trong suốt — header/sidebar + logo kiosk 5tayHCC.opt.webp) và favicon-master.webp (khung tròn đỏ — chỉ favicon/app icon, vì logo trong suốt biến mất ở cỡ 16x16). KHÔNG sửa tay bản SAO trong public/*/favicon*, app-shell/src/assets/images/brand/, 5tayHCC.opt.webp. Đổi: thay đúng file nguồn → build-brand-assets.mjs --apply → sync-brand-assets.mjs --apply.
- kiosk 'npm run lint:js' ĐÃ SỬA 31/07 — chạy được rồi, KHÔNG cần workaround 'npx eslint --config' nữa. Nếu lại thấy 'expand is not a function' thì có ai đó gỡ override lồng minimatch@3 trong frontend-kiosk/package.json (xem AGENTS_LOG).
- HẠ TẦNG ĐÃ DỰNG XONG 31/07: 6 subdomain sống (api/dieuhanh/quantri/kiosk/id/portal), www→301 dieuhanh, apex→301 portal, SSO ĐÃ BẬT. Cổng thông tin ở portal.xanuicam.vn chứ KHÔNG phải www. (lệch có chủ đích, để cứu bookmark cũ) — đừng sửa cho khớp kế hoạch. Chi tiết: docs/DEPLOYMENT/SUBDOMAIN_PAGES_SETUP.md.
- Pha 4c xong: đường dẫn quản trị cũ của dieuhanh. chuyển 301 sang quantri. Chạy local muốn thử thì đặt VITE_QUANTRI_URL=http://localhost:3002, không đặt thì nó nhảy sang production.
- app-shell: shell KHÔNG được import ngược vào app — chạy 'npm run lint:layering' ở frontend/ trước khi commit. File Ở TRONG app-shell cũng không được dùng alias '@shell/...' (tự trỏ vào mình), phải đường dẫn tương đối. Lint gói chạy RIÊNG: 'npm run lint' TRONG packages/app-shell (flat config từ chối file ngoài base path); devDeps ở đó chỉ để lint, KHÔNG thêm react. Alias @dieuhanh đã XOÁ ở pha 4c — đừng dựng lại.
- Kiosk gọi endpoint không tồn tại: ĐÃ SẠCH 31/07 (Xuất báo cáo /admin; nút Nhập Excel GỠ HẲN; /general xuất file ở trình duyệt; và apiUrl('analytics') GỠ HẲN — chốt KHÔNG thu analytics kiosk, thống kê chỉ nằm ở localStorage). Đừng nối lại fetch analytics nếu chưa có route + bảng lưu thật.
- Chạy test kiosk: `PORT=8016 npm run test:e2e`. Để mặc định 8005 mà đang có vite dev của agent khác giữ cổng thì test báo ~166 asset "hỏng" GIẢ (server test không bind được, request rơi vào server cũ). Chẩn: `ss -ltnp | grep 8005`.
- COOKIE_DOMAIN CỐ Ý để trống — same-origin mỗi subdomain (cookie host-only + SameSite=strict, zero CORS); set nó = quay lại mô hình đã loại vì bảo mật (kế hoạch mục 2.2). Hệ quả với kiosk ĐÃ GỠ ở pha 7 mục (c): /admin + /dieukhien của kiosk. lấy phiên qua SSO (trang hạ cánh /auth/). VITE_LOGIN_URL chỉ còn là đường lùi một-origin, KHÔNG cấp được cookie cho origin kiosk.
- Test kiosk (KIỂM CHỨNG LẠI 31/07 chiều): test:e2e 265/265 · test:mounted 15/15 · test:browser 8 lỗi/15 trang và CẢ 8 đều là 401 khi chưa đăng nhập (/admin 7, /dieukhien 1) - không còn lỗi JS thật. Cần backend :8080. CHẠY LẠI KHÔNG CẦN TẢI CHROME: nó đã nằm sẵn ở frontend-kiosk/chrome/ và .tools/libs (đã cho vào .agents-cleanup-keep). Chỉ cần: cd frontend-kiosk && export CHROME_PATH=$PWD/chrome/linux-151.0.7922.71/chrome-linux64/chrome && export LD_LIBRARY_PATH=$PWD/.tools/libs/usr/lib/x86_64-linux-gnu. Mất libs thì: apt-get download libnss3 libnspr4 libasound2t64 → dpkg -x vào .tools/libs (KHÔNG cần sudo).
- `kiosk.zip` (35MB, gitignored) là bản sao DUY NHẤT của 4 file `.sqlite` dữ liệu công dân. `agents.sh cleanup` đã bỏ qua nó qua `.agents-cleanup-keep`. Nạp dữ liệu: `npm run kiosk:load` ở `backend/`.
- Dark-mode ĐÃ XONG CẢ HAI ĐỢT 31/07: literal THUẦN (218 class/65 file) + literal có nội suy `${…}` (61 class/14 file, commit 593f5f8). Quét lại còn 0 chỗ thiếu — ĐỪNG chạy lại codemod. Quy ước: `dark:hover:` (KHÔNG phải `hover:dark:`). Trước khi kết luận "thiếu dark", nhớ 2 sai dương: `dark:` gắn SAU nội suy là áp vô điều kiện (đã đúng), và cặp "X dark:Y" liền kề trong source thường là nhiễu.
- Thư mục làm việc DÙNG CHUNG một HEAD giữa các agent → kiểm tra `git branch --show-current` TRƯỚC khi commit; KHÔNG `git add -A`, chỉ add file cụ thể.
