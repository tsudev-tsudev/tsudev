{
  // Solution file RIÊNG cho backend. Image ở docker/backend-service.Dockerfile
  // chỉ COPY `packages/` và `services/` — không có `apps/`, nên không có `next`,
  // và `@types/react` (vốn chỉ tới được qua hoisting transitive từ next) cũng
  // không có. Dựng `packages/ui` trong image đó vừa vô ích vừa vỡ build.
  //
  // `tsconfig.json` gốc vẫn dựng TẤT CẢ — đó là thứ CI và máy dev dùng.
  "files": [],
  "references": [
    { "path": "packages/types" },
    { "path": "packages/trust-crypto" },
    { "path": "packages/auth" },
    { "path": "services/content-service" },
    { "path": "services/storage-service" },
    { "path": "services/trust-service" },
    { "path": "services/auth-service" },
    { "path": "services/backend-bundle" }
  ]
}
