node_modules
.next
.open-next
dist
build
coverage
verify-output

# Thư mục dựng của cargo (packages/trust-crypto). Chứa hàng chục nghìn tệp JSON
# do trình biên dịch sinh ra - không phải mã của repo.
target

# Bản xuất dữ liệu trước migration DROP - dữ liệu, không phải mã.
backup
e2e/test-results
e2e/playwright-report
.vscode
*.log

# Đặc tả dự án do người viết, không phải mã. Prettier hiểu nhầm các mục
# "7.2", "7.3"… là phần tiếp của danh sách phía trên rồi thụt chúng thành
# mục con, và đánh số lại danh sách - tức là làm sai cấu trúc tài liệu.
documents-tsudev.md

# Ngữ cảnh tự nạp mỗi phiên. Prettier escape "_"/"*" bên trong nội dung
# (vd services/** -> services/\*\*) làm sai lệch glob pattern và tên đường dẫn
# - giữ nguyên văn. AGENTS.md KHÔNG nằm ở đây: nó là văn xuôi bình thường.
CLAUDE.md

# `tokens/` nay CHỈ còn phần mở rộng riêng của web (QU-STD-1, 26/08/2026); bảng
# dùng chung sống ở `.standards/tokens/` bên dưới. Prettier được phép định dạng
# phần mở rộng đó - nó không đi ra ngoài repo này nên không có bản gốc nào để lệch.
#
# `packages/ui/src/tokens.css` cũng KHÔNG nằm ở đây: nó là bản SINH RA cho riêng
# web, và `npm run tokens:check` mới là thứ canh nó.

# Bản sao chỉ-đọc của bộ quy ước trung tâm. TUYỆT ĐỐI không định dạng lại:
# mọi thay đổi ở đây làm lệch MANIFEST.sha256 và khiến
# ./scripts/sync-standards.sh --check báo lệch vĩnh viễn.
.standards/
