# PHIẾU BÀN GIAO - Phiên 19: xác nhận Phase A + B1 next@16 (chặn upstream, revert)

- **Mã phiếu**: 20260822-06
- **Từ**: phiên 19 (22/08/2026) - **Đến**: phiên 20
- **Thời điểm**: 22/08/2026
- **Trạng thái**: ĐÓNG (kết phiên 19 - xem §6)

> Chủ dự án yêu cầu "làm mọi task tới hoàn thiện". Phiên 19: (1) xác nhận PR #60
> (Phase A) đã MERGED + phát hành; (2) làm **B1 next@16** tới cùng - giải xong
> 4/5 blocker nhưng **chặn cứng ở opennextjs** (không bundle được next@16), đã
> REVERT; (3) C1 KHÔNG làm (gated-prod). Chi tiết B1 + lời giải 4 blocker: memory
> `nang-cap-next16-express5`.

## 1. Việc đã làm xong (phiên 19)

- **Xác nhận Phase A phát hành.** PR #60 đã được chủ dự án merge vào `origin/main`
  (`f994ed4`, 07:40Z, CI 6/6 xanh). Local main cũ ở #59 - đã fetch xác nhận. Backend
  live: `/api/trust/verify/<rác>` → 401 JSON sạch (phản hồi bình thường, không sập).
- **B1 next@16 - GIẢI XONG 4 blocker, CHẶN ở #5, REVERT.** Nhánh `feat/next16-upgrade`.
  `next build --webpack` với next@16 **CHẠY XANH** (đường CI "Build frontends" sẽ
  qua). Bốn blocker đã giải (chi tiết + cách làm ở memory):
  1. Turbopack default → `next build --webpack`.
  2. React #31 /admin prerender → dedup MỘT react 19 (packages/ui 18→19 + Storybook
     7→8.6.18 vì SB7 chặn react19; gỡ webpack alias react cũ ở `next.config.js`).
  3. next-auth split SessionContext (`useSession` undefined khi prerender) → một
     bản next-auth ở root (root devDep + peer packages/ui, KHÔNG dev-dep packages/ui).
  4. npm 10.9.8 rớt dep khi tái sinh lockfile → giữ lock main, `npm install` tăng
     dần + `npm dedupe`.

## 2. CHẶN CỨNG (lý do B1 chưa shippable)

- **`@opennextjs/cloudflare@1.20.2` (bản MỚI NHẤT, chỉ dist-tag `latest`, KHÔNG
  canary) KHÔNG bundle được next@16.** `opennextjs-cloudflare build` chết **55 lỗi
  esbuild** "Could not resolve" ở `.open-next/node_modules/next/dist/server/next-server.js`
  (`./node-environment`, `./node-polyfill-crypto`, `../shared/lib/utils`,
  `./request-meta`…). Thử cả **next@16.2.12** (sàn peer opennextjs) lẫn **16.3.2** -
  cùng lỗi. Peer opennextjs khai `next >=16.2.11` nhưng thực tế chưa bundle được
  layout internal của next@16.
- Prod chạy Cloudflare Workers QUA opennextjs (`scripts/deploy-frontend.js` →
  `opennextjs-cloudflare build && deploy`). Không bundle được ⇒ **không deploy
  được** ⇒ B1 bất khả từ phía app. **Không vá được ở đây** - chờ opennextjs ra bản
  hỗ trợ next@16.

## 3. Việc dang dở + bước tiếp theo CỤ THỂ

- [ ] **B1 - chờ opennextjs.** Điều kiện thử lại: `npm view @opennextjs/cloudflare
version` > 1.20.2 (hoặc có dist-tag hỗ trợ next@16). Khi đó: áp lại 4 lời giải
      ở memory `nang-cap-next16-express5` (áp CÙNG LÚC), chạy `opennextjs-cloudflare
build` để xác nhận, rồi full CI + e2e 20/20 (`--workers=1`) + mắt người +
      nghiệm thu prod. next@16 vẫn còn 2 mặt warning chưa xử (không chặn build):
      Sentry "Critical dependency" + edge `process.cwd`; và middleware→proxy
      (deprecation, đổi tên đụng CSP nonce/băm - cẩn thận, có test `csp.test.ts`).
- [ ] **C1 - gated-prod** (chủ dự án). KHÔNG làm phiên này: chỉ nghiệm thu prod
      HTTPS, ship mù = vỡ style/upload IM LẶNG.
- [x] **Phase 0 - đăng nhập GitHub thật** ✅ chủ dự án xác nhận CHẠY trên prod
      (22/08, cuối phiên 19) - auto-link OAuth hoạt động.
- [ ] **Phase 0 - còn 1 mục mắt người** (chủ dự án): DevTools prod /admin,/login
      Console phải sạch dòng CSP vi phạm (runtime HTTPS, curl không đo được).

## 4. File liên quan / trạng thái cây

- Cây làm việc = **main** (đã revert B1 sạch): 0 diff code vs `origin/main`,
  `package-lock.json` khớp main, app next 15.5.22. Chỉ khác: `logs/` (phiếu này +
  STATE + LOCKS).
- LOCKS.md trống (đã nhả hết).

## 5. Cảnh báo / quyết định quan trọng

- **B1 = measured-then-reverted (lần 2).** Đừng đọc "đã giải 4 blocker" thành "đã
  nâng". Cây ở next@15. 4 lời giải chỉ có giá trị khi opennextjs hỗ trợ next@16.
- **Blocker thật của B1 KHÔNG phải 4 mặt phiên 18 liệt kê** - đó chỉ là next
  build. Blocker shippable là **opennextjs** (phiên 18 chưa chạy opennextjs build
  nên chưa thấy). Ghi rõ để phiên sau không lặp lại "giải 4 blocker rồi tưởng xong".
- **Prod hiện KHÔNG có CVE reachable** (sharp/qs đều không với tới). B1 là dọn
  audit + vá CVE next@15, KHÔNG chặn production.

## 6. Kết quả xử lý (phiên sau điền)

- **Cuối phiên 19**: chủ dự án xác nhận **đăng nhập GitHub thật CHẠY trên prod**
  (auto-link OAuth OK) → đóng 1/2 mục mắt người Phase 0. Còn lại: DevTools Console
  sạch CSP. Bàn giao kết phiên theo AGENTS.md §5 - LOCKS trống, cây = main (chỉ
  log), phiếu này ĐÓNG.
