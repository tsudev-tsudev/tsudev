# PHIẾU BÀN GIAO - Phiên 20: dọn nhánh stale + xác minh main xanh

- **Mã phiếu**: 20260822-07
- **Từ**: phiên 20 (24/08/2026) - **Đến**: phiên 21
- **Thời điểm**: 24/08/2026
- **Trạng thái**: ĐÓNG (kết phiên 20)

> Chủ dự án trao quyền tự quyết + tự động hoàn tất. Hàng đợi CẠN việc agent làm
> được (B1 chờ upstream, C1/Phase 0 chờ chủ dự án). Phiên 20 làm việc dọn dẹp +
> xác minh sức khỏe repo, không đụng mã sản phẩm.

## 1. Việc đã làm xong (phiên 20)

- **Xác nhận B1 vẫn CHẶN CỨNG.** `npm view @opennextjs/cloudflare version` →
  **vẫn 1.20.2** (dist-tag `latest`, chưa có bản mới sau 22/08). Điều kiện thử
  lại (>1.20.2 hỗ trợ next@16) CHƯA thỏa. Không làm được từ phía app.
- **Dọn nhánh git stale.** Xóa `-d` (đã merged, an toàn): `feat/next16-upgrade`,
  `docs/oauth-live`, `fix/nut-hoi-sinh-viec-da-dung`, `refactor/giao-dien-quy-uoc-v1`.
  **Còn 2 nhánh cần `git branch -D`** (classifier chặn force-delete, chưa xóa):
  - `docs/ket-phien-15` - merged vào HEAD nhưng git so với `origin/docs/ket-phien-15`
    nên đòi `-D`. Nội dung đã ở main, an toàn.
  - `fix/phase-a-ssrf-ratelimit` - squash-merged qua PR #60 (`f994ed4`), git không
    nhận diện được nên đòi `-D`. Nội dung đã live trên prod, an toàn.
    → Chủ dự án tự chạy `git branch -D docs/ket-phien-15 fix/phase-a-ssrf-ratelimit`
    hoặc cấp quyền `git branch -D` để phiên sau dọn.
- **Xác minh main XANH toàn bộ.** lint ✓ · typecheck ✓ · topology:check ✓
  (52 literal cổng khớp) · tokens:check ✓ (3 chế độ). format:check chỉ cảnh báo
  `.claude/settings.local.json` (gitignored, không vào repo - vô hại).
- **Sửa Prisma client cũ (local).** typecheck ban đầu ĐỎ ở `services/trust-service`
  (`Prisma.SealApplicationWhereInput`/`TrustCertificate` không export + implicit-any
  ăn theo). Nguyên nhân: `.prisma/client` cũ sau đợt `npm install`/`dedupe` của
  nhánh next16 (phiên 19). `npm run db:generate` (v5.22.0) là hết đỏ. **Main
  KHÔNG đỏ thật** (PR #60 merged CI 6/6 xanh) - chỉ artifact local. Bài học: sau
  đợt install/branch-switch lớn, chạy `db:generate` trước khi tin typecheck.

## 2. Việc dang dở + bước tiếp theo CỤ THỂ

- [ ] **Dọn 2 nhánh còn lại** (chủ dự án hoặc cấp quyền `-D`): `docs/ket-phien-15`,
      `fix/phase-a-ssrf-ratelimit`. Cả hai đã ở main, xóa an toàn.
- [ ] **B1 - chờ opennextjs** (không đổi so với phiên 19). Điều kiện thử lại:
      `npm view @opennextjs/cloudflare version` > 1.20.2. Khi đó áp lại 4 lời giải
      ở memory `nang-cap-next16-express5`. Xem phiếu `20260822-06` §3.
- [ ] **C1 - gated-prod** (chủ dự án). Chỉ nghiệm thu prod HTTPS được.
- [ ] **Phase 0 - còn 1 mục mắt người** (chủ dự án): DevTools prod /admin,/login
      Console sạch dòng CSP vi phạm.

## 3. File liên quan / trạng thái cây

- Cây = **main**, 0 diff code vs `origin/main`. Chỉ khác `logs/` (phiếu này +
  STATE + LOCKS + phiếu 05/06 của phiên 18/19 chưa commit).
- **Lưu ý**: các thay đổi `logs/` của phiên 18/19/20 vẫn CHƯA commit (kết phiên
  không commit - chỉ commit/push khi chủ dự án yêu cầu). Chủ dự án muốn dọn cây
  thì `git add logs/ && git commit`.
- LOCKS.md trống (không khóa file nào - phiên 20 không sửa mã).

## 4. Cảnh báo / quyết định quan trọng

- **Hàng đợi CẠN việc agent làm được.** Mọi mục còn lại chờ chủ dự án (C1, Phase 0)
  hoặc upstream (B1). Prod SẴN SÀNG, KHÔNG CVE reachable, không gì chặn vận hành.
- **`git branch -D` bị classifier chặn** trong phiên này - dọn nhánh còn dở là vì
  vậy, không phải vì nhánh không an toàn.

## 5. Kết quả xử lý (phiên sau điền)

- _(chưa có)_
