# PHIẾU BÀN GIAO — Phiên 14: hệ AUTHOR/OWNER + trang quản lý tài khoản, phát hành prod

- **Mã phiếu**: 20260821-04
- **Từ**: phiên 14 (21/08/2026) — **Đến**: phiên 15
- **Trạng thái**: MỞ

> **Phiếu vào cửa của phiên 15.** [`20260821-03`](20260821-03_ket-phien-13.md) đã
> đóng. Hàng đợi ở [`logs/STATE.md`](../STATE.md). Tính năng đã **PHÁT HÀNH prod**
> (backend Render Live, frontend Cloudflare); việc còn lại chủ yếu cần MẮT NGƯỜI.

## 1. Việc đã làm xong

- **Đổi tên 4 nhân sự Toà soạn** (`packages/db/prisma/seed-newsroom.js`, chỉ
  `displayName`): scout-01 "Thợ săn tin" · writer-01 "Biên tập viên" · editor-01
  "Tổng biên tập" · seo-01 "Chuyên viên Marketing". `title`/`systemPrompt` giữ
  nguyên (quyết định chủ dự án). Lan sang DB qua `upsert.update`.
- **Hệ vai trò AUTHOR/OWNER** (chuỗi xuyên vùng trọn gói):
  - `packages/types`: thang `GUEST<MEMBER<VIP<AUTHOR<MODERATOR<ADMIN<OWNER`
    (AUTHOR trên VIP để khách Con dấu không đăng bài; OWNER là trần).
  - `schema.prisma` enum Role + migration `20260821200000_add_author_owner_roles`
    (`ALTER TYPE ADD VALUE`, áp sạch trên Neon). `seed.js`: `tsudev`→OWNER.
  - `services/auth-service`: 6 endpoint `/api/identity/useradmin/*` gác
    `requireOwner` (đọc DB, fail closed): list·create·update·role·revoke·delete.
    **Bất biến**: OWNER không cấp được qua dữ liệu (`ASSIGNABLE_ROLES` bỏ
    OWNER/GUEST); không tự-hạ/tự-xoá; không đụng OWNER khác; `passwordHash` không lộ.
  - `apps/frontend-main`: trang `/admin/accounts` (OWNER-gated) + proxy
    `useradmin/*` + thẻ owner-only ở `/admin`.
  - Test `useradmin.test.ts` (12) canh bất biến leo thang đặc quyền.
- **Hotfix OWNER≥ADMIN (PR #42, `e759dce`)**: nâng OWNER làm lộ regression —
  `auth-service requireAdmin` so `role === 'ADMIN'` BẰNG ĐÚNG nên OWNER bị 403 ở
  mã mời Con dấu. Đổi sang `hasAtLeastRole`; + 2 chỗ trust UI `alreadyIn` →
  `hasAtLeastRole(role,'VIP')`; + test regression trong `invite.test.ts`.
- **Đính chính `CLAUDE.md`**: repo `private`→`Public`; gotcha thang role + hai
  đường nâng vai trò (mã mời VIP + trang tài khoản, đều chặn trần TRONG MÃ).
- **PHÁT HÀNH prod** (chủ dự án chạy): migrate Neon (DIRECT URL) · seed-newsroom
  prod · SQL `tsudev`→OWNER · deploy Cloudflare (`/api/auth/providers` chỉ
  credentials+passkey) · backend Render **Live** (`e759dce`).
- **Nghiệm thu**: typecheck·lint·format sạch; auth-service **74/74**; bundle 14/14;
  migration + seed verify trên DB thật; PR #40/#41/#42 CI xanh; main `e759dce` xanh.

## 2. Việc dang dở + bước tiếp theo CỤ THỂ

- [ ] 🟡 **Nghiệm thu RBAC bằng MẮT NGƯỜI trên prod** — tsudev (OWNER) tạo tài
      khoản thử từng vai trò qua `/admin/accounts` (mật khẩu tự đặt, xoá sau),
      đối chiếu ma trận: OWNER=mọi thứ · ADMIN=admin nhưng KHÔNG accounts ·
      MODERATOR/AUTHOR/VIP=Con dấu, không admin · MEMBER=không gì. Kiểm tsudev
      cấp/thu hồi mã mời Con dấu chạy (hết 403 sau `e759dce` Live). **KHÔNG** bơm
      tài khoản dev (alice/bob) vào prod — chúng là fixture dev, không tồn tại prod.
- [ ] ⚪ **Trình soạn/đăng bài cho AUTHOR (follow-up)** — role AUTHOR đã có nhưng
      content-service CHƯA có route ghi Post cho người. Chuỗi sau: content-service
      thêm route ghi Post gác `requireRole('AUTHOR')` (scope theo `authorId`) →
      frontend thêm trang soạn bài. Khi đó AUTHOR mới thực sự đăng được.
- Các mục cũ còn treo: rà giao diện Storybook (mắt người); tick cron tự nhả
  `NEWSROOM_TICK_TOKEN`; quyết định Issue #1/#2 ở repo trung tâm.

## 3. File liên quan / đang khóa

**Không còn khoá nào** — `logs/LOCKS.md` trống. Cây `tsudev` ở `main` (`e759dce`);
`logs/STATE.md` sửa trong phiên này (commit khi chủ dự án yêu cầu). Bề mặt
`/api/identity/useradmin/*` khớp BỐN chỗ (route+`ASSIGNABLE_ROLES` ·
proxy `ALLOWED` · `useradmin.test.ts` · trang `accounts.tsx`) — đổi phải sửa cả bốn.

## 4. Yêu cầu gửi agent đang giữ khóa

Không có.

## 5. Cảnh báo / quyết định quan trọng

- **Thêm bậc vai trò TRÊN bậc cũ phải rà mọi cổng của bậc cũ** — cổng nào so
  `role === 'ADMIN'` bằng đúng thay vì `hasAtLeastRole` sẽ khoá bậc cao hơn. Đã
  vá `requireAdmin`; nếu thêm bậc trên OWNER sau này, rà lại toàn bộ.
- **Đừng đẩy fix vào PR đang có thể bị gộp đồng thời** — #40 bị gộp ở head cũ
  `449b2f9` (còn lỗi) do GitHub trễ đồng bộ head PR; fix kẹt trên nhánh, main đỏ,
  phải mở #41 vá. Trước khi gộp: đối chiếu `git ls-remote` (branch) ==
  `pulls/N .head.sha` (PR); chờ CI xanh hẳn.
- **"Tài khoản đăng bài" ≠ có bề mặt đăng bài** — AUTHOR chưa đăng được gì tới
  khi có editor (follow-up). Đừng chẩn nhầm là lỗi RBAC.
- **OWNER chỉ cấp bằng seed/DB**, không endpoint nào cấp được. Nâng tài khoản
  khác lên OWNER trên prod = SQL tay, giống lúc nâng tsudev.

## 6. Kết quả xử lý

_(phiên 15 điền tiếp nếu cần)_
