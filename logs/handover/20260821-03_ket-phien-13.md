# PHIẾU BÀN GIAO — Phiên 13: repo Public, CI hồi sinh, repo quy ước trung tâm

- **Mã phiếu**: 20260821-03
- **Từ**: phiên 13 (21/08/2026) — **Đến**: phiên 14
- **Trạng thái**: MỞ

> **Phiếu vào cửa của phiên 14.** [`20260821-02`](20260821-02_ket-phien-12.md) đã
> đóng. Hàng đợi việc ở [`logs/STATE.md`](../STATE.md).

## 1. Việc đã làm xong

- **Repo `tsudev` chuyển PUBLIC** — khắc phục lỗi 🔴 GitHub Actions chết vì tài
  khoản (repo Public được Actions miễn phí không giới hạn phút). Chủ dự án chạy
  `gh repo edit … --visibility public` (cờ `--accept-visibility-change-consequences`
  KHÔNG có ở bản `gh` máy này — bỏ đi). Nghiệm thu `visibility=PUBLIC`.
- **CI hồi sinh, 5/5 job XANH** — chạy lại run `32473196835`: trước Public fail
  4s (không chạy dòng nào); sau Public thực thi thật → Lint · E2E smoke · Build
  frontends · Migrate & test · WASM đều **success**.
- **Quét secret TRƯỚC khi Public** — cây HEAD + toàn bộ **164 commit history**:
  sạch. Chỉ có mẫu/placeholder (`changeme`, `change-me-secret`, `dev-secret`),
  fixture test (`mot-mat-khau-du-dai-2026`, `KEY_A.privB64`), IP tài liệu
  (`203.0.113.9`), và chuỗi con tình cờ (`re_`/`sk-`) trong blob nhị phân.
  `mB50…` chỉ là tiền tố token đã xoay/vô hiệu. `backup/` không track;
  `.env*` bị ignore trừ mẫu.
- **Tạo repo quy ước trung tâm** `tsudev-tsudev/tsudev-standards` (**Private**).
  Lý do Private: repo không chạy workflow nào ⇒ 0 phút Actions ⇒ giới hạn 2.000
  phút/tháng của Private không bao giờ chạm.
- **Bootstrap 10 file** (`91038af`, đã push `main`): `AGENTS.md` (Phần A — bộ quy
  ước v1.0.0, đã bỏ Phần B phân vai riêng repo) · `docs/DESIGN_SYSTEM.md` ·
  `docs/PROJECT_STRUCTURE.md` · `docs/templates/HANDOVER.md` · `tokens/tokens.css`
  · `tokens/design-tokens.json` (chỉ khối dùng chung, bỏ `extensions.tsudev-web`)
  · `README.md` (mới) · `CHANGELOG.md` · hai proposal.
- **Hai gói đẩy ngược mở thành Issue** ở repo trung tâm:
  [#1](https://github.com/tsudev-tsudev/tsudev-standards/issues/1) token WCAG ·
  [#2](https://github.com/tsudev-tsudev/tsudev-standards/issues/2) cấu trúc monorepo.
- **Nhật ký**: STATE.md (đóng mục 🔴 Actions + 🟠 upstream), HANDOFF.md §0 (phiên 13).

## 2. Việc dang dở + bước tiếp theo CỤ THỂ

Hàng đợi việc agent làm được **lại cạn**. Còn lại cần MẮT NGƯỜI / chờ nhịp / QUYẾT ĐỊNH:

- [ ] 🟡 **Rà giao diện bằng MẮT NGƯỜI** — `npm --workspace packages/ui run storybook`,
      nút **Giao diện** đổi ba chế độ. Máy đã rà tương phản; người rà "cân đối chưa".
- [ ] ⚪ **Xác nhận cron worker tự nhả `NEWSROOM_TICK_TOKEN`** (điểm chưa đóng từ
      phiếu 12 §6) — chờ một tick phút-7 UTC tự nhiên rồi `npm run newsroom:check`
      thấy AgentRun tự tăng (KHÔNG gọi tay). Nghiệm thu tay trước đó đã 202.
- [ ] ⚪ **Quyết định ở repo trung tâm** cho Issue #1 (token WCAG) và #2 (cấu trúc)
      — sửa gốc bộ quy ước rồi đồng bộ xuống repo con.
- [ ] ⚪ **`docs-curator` sửa `CLAUDE.md`** dòng đầu _"Repo: private"_ → nay Public
      (xem §5). Không sửa giữa phiên vì là file quy ước.

## 3. File liên quan / đang khóa

**Không còn khoá nào** — `logs/LOCKS.md` trống. Cây `tsudev` ở `main` (`bc17b18`),
chỉ còn `logs/STATE.md` sửa trong phiên này (sẽ commit khi chủ dự án yêu cầu).
Staging của repo trung tâm nằm ở scratchpad, đã push, không cần giữ.

## 4. Yêu cầu gửi agent đang giữ khóa

Không có.

## 5. Cảnh báo / quyết định quan trọng

- **`CLAUDE.md` nay nói SAI về visibility** — dòng đầu ghi "Repo: private"; repo
  đã Public từ 21/08. Cần `docs-curator` đính chính. Không sửa giữa phiên vì
  `CLAUDE.md`/`AGENTS.md` là ngữ cảnh tĩnh cache đầu phiên.
- **Public phơi TOÀN BỘ history công khai** — 164 commit gồm cả nhật ký sự cố bảo
  mật cũ (backdoor `AUTH_DEV_BYPASS`, kiến trúc Keycloak đã gỡ). Không có khoá
  sống, nhưng lộ chi tiết nội bộ. Đã cân nhắc và chủ dự án đồng ý đánh đổi lấy
  Actions miễn phí.
- **Bài học đo đúng**: "billing đã thông chưa" KHÔNG suy từ việc repo thành Public
  — phải ĐẾM job thực thi (in_progress/success) thay vì fail-4s. Chạy lại một run
  cũ là phép nghiệm thu rẻ hơn chờ push mới.
- **`gh` máy này cũ** — không có cờ `--accept-visibility-change-consequences`.
  Đổi visibility chỉ cần `--visibility public`.

## 6. Kết quả xử lý

- **21/08 (phiên 14)** — Đã đính chính `CLAUDE.md` (§2 mục cuối, §5 cảnh báo 1):
  dòng bản đồ `private` → `Public (từ 21/08/2026)`; dòng branch-protection cập
  nhật lý do (Public thì GitHub Free có branch protection nhưng chưa bật). Sửa ở
  cuối việc để tránh bust cache đầu phiên. Ba mục `[ ]` còn lại vẫn chờ mắt
  người / nhịp cron / quyết định repo trung tâm.
