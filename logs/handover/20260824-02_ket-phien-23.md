# PHIẾU BÀN GIAO - Kết phiên 23: phát hành editor nâng cấp (backend+DB) + QU-STD-2

- **Mã phiếu**: 20260824-02
- **Từ**: phiên 23 (24/08/2026) - **Đến**: phiên 24 (hoặc chủ dự án chạy nốt frontend deploy)
- **Thời điểm**: 24/08/2026
- **Trạng thái**: ĐANG XỬ LÝ (backend+DB đã phát hành; CÒN 1 bước = deploy frontend Cloudflare)
- **Nhánh git**: `main` (`7f3b622`). Hai nhánh feat/chore đã merge + xoá remote.

## 1. Việc đã làm xong

- **QU-STD-2** (PR #69, squash `24e7805`): xoá 3 bản sao quy ước v1.0.0 lỗi thời
  (`docs/DESIGN_SYSTEM.md`, `docs/PROJECT_STRUCTURE.md`, `docs/templates/HANDOVER.md`)
  vì `.standards/docs/` đã là v2.0.0; repoint 12 tham chiếu sống sang `.standards/docs/…`;
  `.prettierignore` bỏ 3 dòng chết. **Template handover nay ở `.standards/docs/templates/HANDOVER.md`.**
- **Đóng gói editor Pha 1-7** (PR #68, squash `7f3b622`): toàn bộ chương trình
  code-complete của phiên 21-22. **Fix CI thật**: thêm `export {}` vào
  `services/content-service/test/authoringEnhancements.test.ts` - thiếu nó thì `tsc -b`
  (pretest) gộp biến top-level trùng tên với `softDelete.test.ts` ⇒ TS2451
  "Cannot redeclare" ⇒ job "Migrate & test services" đỏ, dù jest local xanh (bẫy
  CI-bắt-local-giấu, cùng loại `ec907e4`). Cả hai PR CI 7/7 xanh trước khi merge.
- **Phát hành backend + DB lên PRODUCTION** (chủ dự án cấp Neon creds + trao quyền tự chạy):
  1. `prisma migrate deploy 20260824043047` trên Neon prod. **Dùng DIRECT_URL** (non-pooled)
     vì datasource ở `schema.prisma` KHÔNG khai `directUrl` và pooler không giữ được advisory
     lock của migrate. `migrate status` cuối = "Database schema is up to date!".
  2. Merge #69 + #68 vào `main` ⇒ **Render autoDeploy** dựng lại `tsudev-backend`
     (render.yaml KHÔNG khai `autoDeploy` ⇒ mặc định `true`).
  3. `npm run db:generate` rồi `search:reindex` trỏ DB prod (pooler URL) = **35 bài**
     backfill `searchTitleNorm/searchBodyNorm`. Idempotent.
  4. Nghiệm thu prod: homepage 200 có nội dung; `/blog` liệt kê bài thật (publishedAt
     backfill = createdAt nên không bài nào bị ẩn); `/api/auth/providers` chỉ
     credentials·passkey·github·google (không rò dev). **Prod XANH, không vỡ.**

## 2. Việc dang dở + bước tiếp theo CỤ THỂ

> Backend + DB đã live và **tương thích ngược** (frontend cũ + backend mới chạy chung
> được: mọi thay đổi additive; endpoint mới `/api/posts/search` chỉ frontend MỚI gọi).
> Vì thế site KHÔNG vỡ trong lúc chờ frontend deploy - chỉ là UI mới chưa hiện.

- [ ] **Deploy frontend Cloudflare** (bước DUY NHẤT còn lại):
      `node scripts/deploy-frontend.js deploy` (KHÔNG gọi thẳng `opennextjs-cloudflare` -
      script này dời `.env.local` ra khỏi bản dựng + tiêm `NEXT_PUBLIC_MAIN_URL` từ topology).
      **Agent bị classifier CHẶN lệnh này** ⇒ chủ dự án tự chạy (gõ
      `!node scripts/deploy-frontend.js deploy` trong phiên để agent nghiệm thu ngay).
      wrangler đã đăng nhập đúng tài khoản chủ dự án (`dev.nguyentrangtinhsu@gmail.com`,
      có `workers_scripts write`).
- [ ] **Nghiệm thu sau deploy frontend** (bắt buộc, bài học #2 - chỉ đo được trên prod HTTPS): 1. `curl -s https://tsudev.com/api/auth/providers` = **CHỈ** credentials·passkey·
      github·google (đai chống rò config dev vào bản dựng). 2. `/search` gõ "tuong tac" (không dấu) ra bài gắn tag; `/author` editor đủ trường;
      một bài blog hiện "Nguồn tham khảo" + ảnh bìa + tag chip bấm được.
- [ ] **Tuỳ chọn**: đặt `PEXELS_API_KEY` ở Render (agent AI tự chèn ảnh bìa; không đặt =
      agent vẫn xuất bản, không ảnh) · kiểm `S3_PUBLIC_ENDPOINT` prod cho GET công khai
      object (ảnh/video nhúng đọc qua URL này).
- [ ] **Commit logs phiên 23**: `logs/STATE.md`, `logs/LOCKS.md`, phiếu này đang là thay
      đổi CHƯA COMMIT trên `main` (agent không tự push - cổng an toàn). Chủ dự án commit
      theo luồng thường (vd `docs(logs): kết phiên 23`).

## 3. File liên quan / đang khóa

| Đường dẫn                                                       | Lý do                      | Còn khóa? |
| --------------------------------------------------------------- | -------------------------- | --------- |
| `logs/STATE.md`, `logs/LOCKS.md`, `logs/handover/20260824-02_*` | Log kết phiên, CHƯA commit | Không     |
| `scripts/deploy-frontend.js`                                    | Lệnh chạy nốt (không sửa)  | Không     |

Không giữ khoá nào (LOCKS.md trống).

## 4. Yêu cầu gửi agent đang giữ khóa (nếu có)

- Không có.

## 5. Cảnh báo và quyết định quan trọng

- **QU-STD-4 KHÔNG được làm - là BẪY, không phải việc vặt.** Mô tả queue là "xoá một
  dòng NEXT_PUBLIC_MAIN_URL khỏi `.env.production`", nhưng file đó được **commit CHỦ ĐÍCH**:
  header của chính nó + `scripts/topology/load.js` (`managedProdEnv`) ghi rõ nó bảo đảm
  "bản dựng trên máy KHÁC hay CI ra cùng kết quả". Giá trị có HAI người tiêu thụ độc lập
  (deploy-frontend.js tiêm shell = đai chống `.env.local` đè; file commit = dây treo cho
  CI/máy khác). Bỏ file = mất dây treo ⇒ canonical/OG/sitemap rỗng ở build không qua
  deploy-frontend.js, "sai mà không báo lỗi". "Sửa dứt điểm" đúng cách cần sửa
  `.github/workflows` inject URL từ topology + nghiệm thu CI + prod - quyết định hạ tầng
  của chủ dự án. Không gấp (miễn trừ `.standards-allow` tới 31/12/2026). Đầy đủ: memory
  `qu-std-4-env-production-bay`.
- **Migration Neon phải dùng DIRECT_URL** (không pooler): datasource không có `directUrl`,
  pooler PgBouncer transaction-mode không giữ advisory lock của `migrate deploy`.
- **Render autoDeploy = true (mặc định)**: mọi push/merge vào `main` dựng lại backend.
  Đã đúng thứ tự lần này (migrate Neon TRƯỚC merge). Lần sau đổi schema: LUÔN
  `migrate deploy` prod trước, rồi mới merge, nếu không backend mới truy vấn cột chưa có
  ⇒ blog trắng (bẫy "200 không chứng minh có nội dung").
- **Bẫy `export {}` trong test**: file test top-level không-module + trùng tên biến với
  file khác ⇒ `tsc -b` đỏ chỉ trên CI. Test mới trong `services/*/test` phải có `export {}`.
- QU-STD-1/3 cần MẮT NGƯỜI (QU-STD-1 đổi màu 3 chế độ = phá vỡ, chụp lại ảnh; QU-STD-3
  phụ thuộc QU-STD-1 vì `border-control` chưa tồn tại tới khi QU-STD-1 xong).

## 6. Trạng thái cổng kiểm

- [x] Cổng chung trước merge: typecheck·lint·format·topology·tokens xanh; CI cả 2 PR **7/7**.
- [x] Test: content **60** (sau fix export{}), ui **199**; các workspace khác xanh trên CI.
- [x] `prisma migrate status` prod = "up to date"; nghiệm thu prod backend/DB xanh.
- [ ] `git status` sạch ngoài phạm vi bàn giao: **còn `logs/STATE.md`+`LOCKS.md`+phiếu này
      CHƯA commit** (chủ đích - agent không push). Không có file rác/temp trong repo.

## 7. Kết quả xử lý (agent nhận điền sau khi thực hiện)

- (chờ) Kết quả `node scripts/deploy-frontend.js deploy` + nghiệm thu providers/`/search`/`/author`.
