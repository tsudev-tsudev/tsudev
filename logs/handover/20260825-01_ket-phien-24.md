# PHIẾU BÀN GIAO - Kết phiên 24: đóng hai đề xuất đẩy ngược + dọn link chết docs

- **Mã phiếu**: 20260825-01
- **Từ**: phiên 24 (25/08/2026) - **Đến**: phiên 25
- **Thời điểm**: 25/08/2026
- **Trạng thái**: **XONG.** Task docs đã đóng; frontend Cloudflare **ĐÃ DEPLOY** trong
  phiên (chủ dự án chạy, agent nghiệm thu - §7). Chỉ còn việc commit.
- **Nhánh git**: `main` (`7f3b622`). Thay đổi phiên này CHƯA commit.

## 1. Việc đã làm xong

**Task nhận**: đóng hai đề xuất đẩy ngược (`docs-curator`) - phát hiện phụ của phiên 23.

### 1.1. Đối chiếu: cả hai đề xuất ĐÃ ĐƯỢC NHẬN nguyên văn vào `.standards/` v2.8.0

Bản đồng bộ tại `.standards/` (ref `v2.8.0`, commit `bf4ea54`, synced 24/08):

| Đề nghị                                              | Trạng thái v2.8.0                                                                                                                      |
| ---------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| `text-muted` đổi giá trị                             | ✅ `#52627A` · `#5E5646` · `#9BB0C9` - khớp từng mã bảng A                                                                             |
| Thêm vai trò `border-control`                        | ✅ `#74899F` · `#8E8064` · `#6E88AE` - có ở `design-tokens.json`, `tokens.css`, `design-tokens.schema.json`                            |
| `border-strong` GIỮ NGUYÊN, thu về vai trò trang trí | ✅ vẫn `#9FB8D4`/`#BCAE90`/`#3F5B80`; `DESIGN_SYSTEM.md` §1 tách hai vai trò + ngưỡng 3:1 WCAG 1.4.11                                  |
| Hình trạng B - monorepo                              | ✅ `PROJECT_STRUCTURE.md` §1 Hình trạng A / §2 Hình trạng B, **thêm** bảng phân biệt `services/` gốc repo vs `src/services/` trong app |

**Issue #1 và #2 ở `tsudev-tsudev/tsudev-standards` đã CLOSE** (reason: completed),
mỗi cái kèm bảng đối chiếu để bên đó không phải đo lại.

### 1.2. File sửa

- `docs/token-upstream-proposal.md` · `docs/structure-upstream-proposal.md`: header
  đổi "chờ gửi" → **"ĐÃ ĐƯỢC NHẬN, đóng 25/08/2026"**, ghi rõ đối chiếu + link issue.
  **Giữ nguyên phần thân** - số đo 4 tầng nền × 3 chế độ vẫn dùng để kiểm chứng lại.
- `docs/README.md`: **2 link CHẾT** (`DESIGN_SYSTEM.md`, `PROJECT_STRUCTURE.md` - bị
  QU-STD-2 xoá ngày 24/08) → trỏ `../.standards/docs/…`; hai dòng đề xuất đánh dấu
  _(hồ sơ đã đóng)_; §"Hai tài liệu viết HOA" **viết lại** (đoạn cũ còn nói "v1.0.0"
  và mô tả hai file như thể vẫn nằm trong `docs/`).
- `docs/architecture.md` §Điểm lệch cần biết: mục cây thư mục **hết lệch** - rút từ
  13 dòng xuống một dòng khai báo Hình trạng B, đúng như đoạn cuối gói đề xuất dự trù.

## 2. Việc dang dở + bước tiếp theo CỤ THỂ

- [x] **Deploy frontend Cloudflare** - ✅ **XONG trong phiên này** (chủ dự án chạy).
      Đầu phiên `/search` còn 404; sau deploy 200. Kết quả + nghiệm thu đầy đủ ở **§7**.
- [ ] **Đặt `PEXELS_API_KEY` ở Render** (tuỳ chọn, nhưng nay là đường rẻ nhất để đóng
      hai mục chưa nghiệm thu được ở §7 - agent AI sẽ tự chèn ảnh bìa + ghi công tác
      giả vào `references`, làm cả hai vùng có dữ liệu thật để đo). Không đặt = agent
      vẫn xuất bản, chỉ không có ảnh.
- [ ] **Kiểm `S3_PUBLIC_ENDPOINT` prod** cho GET công khai object (ảnh/video nhúng đọc
      qua URL này) - còn nợ từ phiếu `20260824-02` §2.
- [ ] **Commit logs + docs phiên 24** (agent không tự push - cổng an toàn). Gợi ý tách 2 commit:
      `docs(standards): đóng hai đề xuất đẩy ngược đã được nhận vào v2.8.0` (4 file `docs/`) + `docs(logs): kết phiên 24` (`logs/`). Lưu ý cây còn mang cả logs phiên 23 chưa commit.
- [ ] **Nợ vùng khác** (KHÔNG tự vượt biên, xem §4).

## 3. File liên quan / đang khóa

| Đường dẫn                                     | Lý do                      | Còn khóa?      |
| --------------------------------------------- | -------------------------- | -------------- |
| `docs/{token,structure}-upstream-proposal.md` | Đổi header sang đã-đóng    | Không (đã trả) |
| `docs/README.md`, `docs/architecture.md`      | Dọn link chết + điểm lệch  | Không (đã trả) |
| `logs/STATE.md`, `logs/LOCKS.md`, phiếu này   | Log kết phiên, CHƯA commit | Không          |

`LOCKS.md` nay trống.

## 4. Yêu cầu gửi agent đang giữ khóa (nếu có)

Không ai đang giữ khóa. Hai việc **thuộc vùng khác**, mô tả để bàn giao thay vì tự sửa:

- **`design-system`** - `tokens/design-tokens.json` khối `extensions.tsudev-web`, khoá
  `$accessibility_gap`: chuỗi này còn nói _"Bảng `color` chuẩn v1.0.0 KHÔNG đạt… CẦN
  ĐẨY NGƯỢC LÊN repo token trung tâm"_. Nay **lỗi thời** - trung tâm đã nhận và vá.
  Viết lại khi làm QU-STD-1 (cùng lúc gỡ phần ghi đè dư thừa).
- **`CLAUDE.md`** mục Tài liệu còn mô tả hai file là _"kèm gói đề xuất gửi ngược lên
  trung tâm"_. Sửa vào **CUỐI phiên** theo quy ước (sửa giữa phiên = bust cache).

## 5. Cảnh báo và quyết định quan trọng

- 🔑 **QU-STD-1 KHÔNG còn là thay đổi phá vỡ về màu - nhãn trong hàng đợi đã hết đúng.**
  Đo được: `diff` khối `color` local vs `.standards/tokens/` chỉ lệch (a) `text-muted`
  (bản local là v1.0.0 cũ) và (b) khoảng trắng trong `rgba()`. Nhưng bản local **không
  hiệu lực**: trong `packages/ui/src/tokens.css` sinh ra, khối `extensions` nằm SAU
  khối `color` **trong cùng scope** (`:root, :root[data-theme='light']` dòng 22-69;
  tương tự warm 73-119, dark 124-170) nên thắng cascade. Giá trị đang render là
  `#52627a`/`#5e5646`/`#9bb0c9` = **đúng giá trị chuẩn mới**. ⇒ Gỡ ghi đè khi di trú
  **không đổi một pixel nào** ở `text-muted`/`border-control`. Nhãn "PHÁ VỠ" có từ
  thời `.standards/` còn v1.0.0/v2.0.0. Việc còn lại là cơ khí: repoint 17 file đọc
  token cục bộ + `tokens:sync` + chụp lại ảnh nghiệm thu.
  ⚠️ Bài học §0.7 #4 vẫn áp dụng: trước khi tin kết luận này, kiểm bằng nguồn thứ hai
  (`packages/ui/test/contrast.test.ts` đang XANH ở ngưỡng AA mà `#5F7891` trượt -
  chính là bằng chứng độc lập rằng giá trị hiệu lực là bản ghi đè).
- **QU-STD-3 nay có thể làm ngay sau QU-STD-1** - `border-control` đã tồn tại ở cả
  `.standards/tokens/` lẫn `tokens/` local (5 chỗ) và `packages/ui/src/tokens.css`
  (3 chỗ), nên phụ thuộc đã được gỡ một nửa.
- **QU-STD-2 để lọt link chết vào mục lục.** Lần đó repoint 12 tham chiếu dạng _text/
  comment_ nhưng bỏ sót _link markdown_ trong `docs/README.md`. Bài học chung: xoá file
  trong `docs/` thì quét cả `](…)`, đừng chỉ grep tên file. Phiên này đã quét lại toàn
  bộ link của `docs/README.md`: 0 chết.
- **QU-STD-4 vẫn KHÔNG làm - là BẪY** (memory `qu-std-4-env-production-bay`, phiếu
  `20260824-02` §5). Miễn trừ tới 31/12/2026.
- **B1 vẫn CHẶN**: `npm view @opennextjs/cloudflare version` = **1.20.2**, y như phiên
  19-20. Điều kiện thử lại không đổi: > 1.20.2.

## 6. Trạng thái cổng kiểm

- [x] `npx prettier --check` 4 file docs đã sửa: **All matched files use Prettier code style**.
- [x] `npm run tokens:check`: "tokens: khớp nguồn (3 chế độ)".
- [x] `npm run topology:check`: 4/4 ✓.
- [x] Quy ước gạch ngang: 0 em-dash + 0 en-dash trong cả 4 file đã sửa.
- [x] Quét link `docs/README.md`: 0 link chết.
- [ ] typecheck/lint/test **không chạy** - phiên này chỉ sửa markdown, không đụng mã
      nguồn hay token. `tokens:check` + `topology:check` là hai cổng duy nhất có thể
      đỏ vì thay đổi loại này, cả hai đã xanh.
- [ ] `git status`: **chưa commit** - `logs/` (phiên 23 + 24) + 4 file `docs/` + phiếu
      này. Chủ đích: agent không push. Không có file rác/temp.

## 7. Kết quả xử lý

**FRONTEND CLOUDFLARE ĐÃ DEPLOY - 25/08/2026, chủ dự án chạy trong phiên 24.**
`node scripts/deploy-frontend.js deploy` → exit 0, **Version ID `2c9dd20c-9ffe-4bd3-948e-3e698b1f92ce`**,
triggers `tsudev.com` + `www.tsudev.com`. Log xác nhận `.env.local` được dời khỏi bản
dựng rồi `↩ đã trả lại` (đai chống rò cấu hình dev chạy đúng). **Chương trình editor
nâng cấp nay LIVE đủ backend + DB + frontend.**

### Nghiệm thu prod - ĐO ĐƯỢC, XANH

| Mục                   | Kết quả                                                                                 |
| --------------------- | --------------------------------------------------------------------------------------- |
| `/api/auth/providers` | `credentials·github·google·passkey` - **không rò dev**                                  |
| `/search`             | 404 (trước deploy) → **200**, 11 KB                                                     |
| Tìm **không dấu**     | `bao mat` = `bảo mật` = **7** · `tuong tac` = `tương tác` = **3** · `sieu may tinh` = 1 |
| Chặn min-2 ký tự      | `q=t` → `data:[]`, `total:0` - rỗng sạch, không lỗi                                     |
| Facet tag             | trả `{slug,count}` thật                                                                 |
| Tag chip + lọc        | `/blog?tag=x-org-server` → 200, đúng 1 bài                                              |
| `/author`             | 200 (không 404)                                                                         |
| Homepage · `/blog`    | 200 **kèm nội dung thật** (33 KB · 130 KB, liệt kê bài)                                 |
| canonical / `og:url`  | `https://tsudev.com` - **0 chuỗi `localhost`** trong HTML                               |

### CHƯA nghiệm thu được trên prod (thiếu DỮ LIỆU, không phải mã hỏng)

- **Vùng render "Nguồn tham khảo"**: quét 12 bài, **0 bài có `references` thật**
  (`"references":[]` trong payload SSR). Bài cũ có sẵn từ trước Pha 6.
- **Ảnh bìa riêng**: 0/12 bài có `coverImageUrl`; mọi bài dùng `og-image.png` mặc
  định - đúng như dự kiến vì `PEXELS_API_KEY` **chưa đặt** ở Render (tuỳ chọn).
- Cách đóng: đặt `PEXELS_API_KEY` rồi chờ agent xuất bản bài mới, **hoặc** đăng/sửa
  một bài qua `/author` (cần đăng nhập - mắt người).

### ⚠️ Hai lần công cụ ĐO tự sinh ra "lỗi" không có thật (bài học §0.7 #4, cùng phiên)

1. Lần đo search đầu: script dò khoá `items`/`results`/`posts`, nhưng API trả
   **`data`** ⇒ in ra `0 kết quả` trong khi search vẫn đúng. Suýt kết luận "search
   hỏng trên prod".
2. `grep -c "Nguồn tham khảo"` bắt **2 bài** - nhưng đó là **văn bản trong thân bài**
   do agent AI viết (`Nguồn tham khảo: https://…` nằm trong `<p>` của nội dung), KHÔNG
   phải vùng render references. Payload SSR của chính bài đó là `"references":[]`.
   Suýt báo "đã nghiệm thu" cho thứ chưa hề chạy.

   **Rút ra**: nghiệm thu tính năng mới trên prod phải đọc **payload dữ liệu**
   (`__NEXT_DATA__`/API), đừng grep chuỗi hiển thị - chuỗi đó có thể do NỘI DUNG sinh
   ra chứ không phải do MÃ sinh ra. Sai theo cả hai chiều: báo hỏng khi đang chạy, và
   báo chạy khi chưa chạy.
