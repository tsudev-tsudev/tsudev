# PHIẾU BÀN GIAO - Phiên 17: Phase A đợt khắc phục triệt để (SSRF + rate limit)

- **Mã phiếu**: 20260822-04
- **Từ**: phiên 17 (22/08/2026) - **Đến**: phiên 18
- **Thời điểm**: 19:10 22/08/2026
- **Trạng thái**: MỞ

> Phiếu vào cửa của phiên 18. Chủ dự án mở đợt khắc phục triệt để; phiên 17 làm
> xong **Phase A** (A1·A2·A3) - CODE-COMPLETE, cổng xanh, **CHƯA phát hành, chưa
> commit**. Hàng đợi còn lại (B1·C1·Phase 0) và toàn bộ chi tiết kỹ thuật nằm ở
> phiếu [`20260822-03`](20260822-03_ke-hoach-khac-phuc-triet-de.md) - đọc phiếu đó
> để làm tiếp, KHÔNG lặp lại ở đây.

## 1. Việc đã làm xong (phiên 17)

- **A1. SSRF `domainVerify`** - vá triệt để. `node:https` + `guardedLookup` (ghim
  IP đã kiểm vào socket, đóng TOCTOU) + redirect thủ công ≤3 chặng chỉ https, mỗi
  chặng kiểm lại (đóng lỗ redirect-SSRF mới phát hiện). Zero-dep. Test
  `services/trust-service/test/domainVerify.test.ts` **30**.
- **A2. npm audit** - điều tra: KHÔNG có bản vá non-breaking (express@4 ghim
  `qs ~6.14.0`; sharp cần next@16). Lỗi qs không với tới được (mã không dùng
  `qs.stringify`). Dời sang B1.
- **A3. Rate limit content + storage qua module dùng chung** - package
  `@tsudev/ratelimit` (trích từ trust-service). Gắn content 300/phút, storage
  120/phút, MIỄN TRỪ lưu lượng mang `x-internal-token` (BFF không forward IP
  client). Test rateLimit.test cho cả hai.
- Cổng chung: typecheck · lint · topology · tokens · format đều sạch. Test:
  trust **87** · content **46** · storage **15**.
- Cập nhật memory `ra-soat-bao-mat` (đính chính "còn nợ" đã lỗi thời).

## 2. Việc dang dở + bước tiếp theo CỤ THỂ

- [ ] **Phát hành Phase A** (cần chủ dự án): commit + PR → merge `main` → Render
      tự dựng lại backend-bundle. Nghiệm thu ở phiếu 03 §3 (direct flood `/api/<x>`
      không token → 429; lưu lượng site qua BFF không 429). KHÔNG cần env mới.
- [ ] **B1** (đợt major, breaking) - phiếu 03 §2. Chờ chủ dự án mở.
- [ ] **C1** (siết CSP, tuỳ chọn) - phiếu 03 §2.
- [ ] **Phase 0** (rẻ) - rà nốt 7/9 lỗi TS-migrate; 2 mục mắt người.

## 3. File liên quan / đang khóa

- LOCKS.md trống (đã nhả hết).
- Thay đổi Phase A CHƯA COMMIT trong working tree - danh sách file đầy đủ ở phiếu
  03 §4 (mới: `packages/ratelimit/*` + 3 test; sửa: trust/content/storage +
  3 tsconfig; xóa: `trust-service/src/rateLimit.ts`).
- `main` đang ở `5bad454` (chưa có thay đổi phiên 17).

## 4. Yêu cầu gửi agent đang giữ khóa

Không có.

## 5. Cảnh báo / quyết định quan trọng

- **Lỗ redirect-SSRF** (nặng hơn TOCTOU) chưa từng ghi trước phiên 17.
- **BFF không forward IP client** xuống service → rate-limit theo IP chỉ có nghĩa
  cho lưu lượng TRỰC TIẾP; phải miễn trừ lưu lượng BFF (`x-internal-token`). Cùng
  lý do, limiter 240/phút của trust-service gộp badge-qua-BFF vào một xô (việc của
  trust-seal cân nhắc sau).
- **A3 = module dùng chung** (chủ dự án chốt). `@tsudev/ratelimit` semicolon-style.
- **B1 breaking** (next@16 + express@5): đợt riêng, đo phơi nhiễm sharp trước.
- Phase A chưa commit theo yêu cầu chủ dự án (kết phiên, để chủ dự án tự review/
  quyết phát hành).

## 6. Kết quả xử lý (phiên sau điền)

- **Phiên 18 (22/08)**: Phase A đóng gói thành PR (chủ dự án cho phép push). Cổng
  chung sạch; test trust 87 · content 46 · storage 15 (chạy với DB local :5433).
  B1 đo-rồi-revert (không ship - cây vẫn next@15). C1 hoãn. Phase 0 code xong.
  Chi tiết: phiếu [`20260822-05`](20260822-05_ket-phien-18.md).
