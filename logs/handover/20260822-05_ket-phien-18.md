# PHIẾU BÀN GIAO - Phiên 18: Phase 0 + đo B1 + phát hành Phase A

- **Mã phiếu**: 20260822-05
- **Từ**: phiên 18 (22/08/2026) - **Đến**: phiên 19
- **Thời điểm**: 21:00 22/08/2026
- **Trạng thái**: ĐÓNG (phiên 19 xác nhận PR #60 merged - xem §6)

> Chủ dự án yêu cầu "làm tất cả task". Phiên 18: đóng **Phase 0** (code), **đo
> B1** (next@16 - measured-then-reverted, không ship), đánh giá **C1** (hoãn),
> và **đóng gói Phase A thành PR** (chủ dự án cho phép push). B1/C1 hoãn theo
> quyết định chủ dự án. Chi tiết B1 ở memory `nang-cap-next16-express5`.

## 1. Việc đã làm xong (phiên 18)

- **Phase 0 (code) - XONG.** Rà 7/9 lỗi TS-migrate còn lại (#3-#9) bằng grep
  nguồn, TẤT CẢ đã vá: #3 `basis` (AssessmentBasis + whitelist enum ở index.ts),
  #4 `certCard` (`c.org ?` guard + GetPayload), #5 Upload.tsx (`err instanceof
Error`), #6 Modal.tsx (`active instanceof HTMLElement`), #7 query/x-filename
  (`qStr` + `Array.isArray`), #8 `routeParam(params,'slug')`, #9 docs category
  (`|| 'Khác'`). Đóng memory `loi-that-typescript-bat-duoc` (giữ tham chiếu).
- **B1 - ĐO XONG, KHÔNG ship** (revert sạch về next@15). next@16.3.2 clear TOÀN
  BỘ CVE high/critical prod (`npm audit --omit=dev` → 3 moderate qs non-reachable).
  Sharp không reachable prod. Migration breaking nhiều mặt (Turbopack default ·
  React #31 dual-copy prerender · Sentry edge · middleware→proxy). Chi tiết đầy
  đủ: memory `nang-cap-next16-express5`.
- **C1 - đánh giá, hoãn.** Cả hai phần chỉ nghiệm thu prod HTTPS; style-src
  unsafe-inline khó bỏ; connect-src cần host R2 presign (secret). Để gated-prod.
- **Phát hành Phase A** - đóng gói + PR (xem §2 dưới nếu chưa merge xong).

## 2. Việc dang dở + bước tiếp theo CỤ THỂ

- [ ] **Merge PR Phase A** (chủ dự án): sau merge `main`, Render tự dựng lại
      backend-bundle. **Nghiệm thu sau deploy** (phiếu 03 §3): `curl` một
      `/api/trust/verify/<serial>` rác → phản hồi bình thường; direct flood
      `/api/<x>` KHÔNG token → 429; lưu lượng site qua BFF không 429. KHÔNG cần
      env mới (ngưỡng có default; siết bằng `RATE_LIMIT_DIRECT_MAX`).
- [ ] **B1 - đợt riêng** (chủ dự án mở). Migration next@16 theo memory
      `nang-cap-next16-express5`: giải React #31 (dual-copy) trước, rồi Turbopack
      alias / hoặc chốt `--webpack` cho cả opennextjs deploy, Sentry edge, cân
      nhắc middleware→proxy. Full CI + e2e 20/20 (`--workers=1`) + mắt người.
- [ ] **C1 - gated-prod** (chủ dự án). Nếu làm: siết `connect-src` cần host R2
      presign thật; `style-src` cần nonce/hash cho style Next/Tailwind.
- [ ] **Phase 0 - 2 mục mắt người** (chủ dự án): DevTools prod Console sạch dòng
      CSP vi phạm; đăng nhập GitHub thật chạy auto-link.

## 3. File liên quan / đang khóa

- LOCKS.md trống (đã nhả hết - B1 đã revert nên các file package.json/lock/
  frontend về đúng trạng thái Phase A).
- Phase A: mới `packages/ratelimit/*` + 3 test; sửa trust/content/storage +
  3 tsconfig; xóa `trust-service/src/rateLimit.ts` (danh sách đầy đủ phiếu 03 §4).

## 4. Yêu cầu gửi agent đang giữ khóa

Không có.

## 5. Cảnh báo / quyết định quan trọng

- **B1 measured-then-reverted**: đừng đọc "đã thử next@16" thành "đã nâng". Cây
  đang ở next@15. Bằng chứng giá trị (audit sạch) + danh sách blocker ở memory.
- **Prod hiện KHÔNG có CVE reachable** (sharp/qs đều không với tới runtime
  Workers/không dùng `qs.stringify`). B1 là dọn audit + vá loạt CVE next@15, KHÔNG
  chặn production.
- **C1 ship mù = hỏng im lặng** (style/upload chỉ vỡ trên prod HTTPS). Không flip
  nếu không nghiệm thu được prod.

## 6. Kết quả xử lý (phiên sau điền)

- **Phiên 19 (22/08)**: PR #60 (Phase A) **ĐÃ MERGED** vào `origin/main`
  (`f994ed4`, 07:40Z, CI 6/6 xanh) - chủ dự án gộp. A1/A2/A3 phát hành, Render
  dựng lại backend-bundle. Nghiệm thu nhẹ: `/api/trust/verify/<rác>` → **401 JSON
  sạch** (backend live, phản hồi bình thường). Flood 429 KHÔNG chạy trên prod
  (không nện 300+ lượt) - để lại cho chủ dự án đo có kiểm soát. Sau đó phiên 19
  **bắt tay B1 next@16** (nhánh `feat/next16-upgrade`) - xem STATE.md.
