# Frontend Forum (Next.js placeholder)

This app is a minimal placeholder for the forum UI. It will integrate with the `content-service` and the central SSO provider.

Run via `docker-compose up frontend-forum` in development.
