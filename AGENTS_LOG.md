# AGENTS_LOG.md — Nhật ký (append-only · CHỈ `grep`/`tail` · ĐỪNG đọc cả file)

Chỉ ghi thứ **GIT KHÔNG thấy được**: quyết định thiết kế, gotcha/cạm bẫy, kết quả
test quan trọng. Muốn biết **"ai sửa gì"** → dùng `git log --oneline [-- <file>]`.

- Append bằng `>>` hoặc `./agents.sh log "…"` (KHÔNG mở/ghi lại cả file).
- Lịch sử 2026-07-26 → 2026-07-28: `AGENTS_LOG_ARCHIVE_2026-07-29.md`.
  Trước 2026-07-26: git log + memory của Claude Code.
- Nén: khi file này > 200 dòng → chuyển đoạn cũ vào `AGENTS_LOG_ARCHIVE_<YYYY-MM-DD>.md`.

Format: `[YYYY-MM-DD HH:MM] agent-X | LOẠI | nội dung`
LOẠI ∈ {QUYẾT ĐỊNH, GOTCHA, TEST, KHÁC}

---

[2026-07-29 01:08] agent-1 | QUYẾT ĐỊNH | VÁ vòng lặp login production (PR #13). RCA: logActivity(audit_logs)+logDeviceLogin(device_login_logs) INSERT trên client transaction của login rồi NUỐT lỗi → khi INSERT lỗi ở prod, transaction ABORTED → COMMIT của controller âm thầm ROLLBACK → mất hàng session → token hợp lệ nhưng findActiveSessionByJti không thấy phiên → 401 me+refresh → văng /login. Local không tái hiện (IP localhost bỏ qua geolocation). FIX: SAVEPOINT bọc 2 INSERT best-effort + timeout 2s cho axios geo. KIỂM CHỨNG live: login→session lưu (Neon 6→7)→/me 200. CÒN LẠI (thứ yếu): audit INSERT vẫn fail trên Render (0 audit mới, giờ bị savepoint cô lập nên không hỏng login) + login chậm 8s — cần Render log (dòng 'Lỗi khi ghi nhật ký hệ thống') để tìm lý do INSERT audit fail. Neon reads LIVE từ Render (đã kiểm chứng đổi mật khẩu).
[2026-07-29 01:16] agent-1 | QUYẾT ĐỊNH | HOÀN TẤT xanuicam.vn. PR #14 vá gốc: audit INSERT lỗi 22001 varchar(45) — X-Forwarded-For sau CF Pages→Render là chuỗi nhiều IP >45 ký tự, cột audit_logs.ip_address VARCHAR(45); fix = lấy IP đầu. Kiểm chứng live: login→session lưu, /me 200, AUDIT GHI ĐƯỢC đúng IP+geo (171.253.230.59|Vietnam). PR #13 (savepoint) là lớp phòng vệ. Login còn ~8s = argon2id (memoryCost 19MB) trên CPU free-tier Render 0.1vCPU — không phải bug, là chi phí hash mật khẩu; nâng plan/VPS sẽ nhanh. Tổng cộng phiên: PR#10 PaaS, #11 mig017, #12 mig018 views, #13 savepoint, #14 audit IP.
[2026-07-29 03:01] agent-1 | GOTCHA | envelope+pagination (PR#17). "Không thể tải dữ liệu" có 2 nguyên nhân ĐỘC LẬP: (1) `handleResponse` chỉ bóc `payload.data` khi payload có ĐÚNG 1 key `data` (hoặc kèm `message`); endpoint phân trang trả `{data,pagination}` = 2 key không message → KHÔNG bóc → trang gọi `.forEach` trên object → TypeError dù HTTP 200. Chữa: `handleListResponse` ở apiService (7 endpoint), KHÔNG đụng `handleResponse` (sẽ làm rơi pagination của DocumentsPage/ComputerConfigsPage). (2) lọc leadership `role LIKE '%<slug>%'` KHÔNG BAO GIỜ khớp (LIKE phân biệt hoa/thường; 'danguy' ≠ 'Đảng ủy') → luôn 0 dòng; nay `role = ANY(<danh sách nhãn thật>)`. Test hợp đồng 2 nửa khoá lại: `middleware/__tests__/responseEnvelope.contract.test.ts` + round-trip trong `services/apiService.test.ts` — sửa 1 bên quên bên kia thì bên kia đỏ.
[2026-07-29 03:01] agent-1 | GOTCHA | tsconfig backend THIẾU `target`: `module:Node16` ngầm định ES2022 nên `tsc` sạch, NHƯNG khối `ts-node` ghi đè `module:commonjs` làm mất suy diễn → target tụt ES5 → mọi script ts-node (db:migrate, db:bootstrap-test, db:seed) chết TS2802 ở utils/totp.ts. Là lý do job CI "Test Suite" đỏ kinh niên. Đã khai báo `target: ES2022` tường minh.
[2026-07-29 03:35] agent-1 | GOTCHA | Đổi cổng local: `PORT=8085 npm run dev` KHÔNG có tác dụng — `config/env.ts` nạp `backend/.env` với `override:true` nên PORT trong file đè ngược biến shell (đã tái hiện: bám :8080 → EADDRINUSE). Đổi cổng backend phải sửa CẢ `backend/.env` LẪN 3 mục proxy hardcode `localhost:8080` trong `vite.config.mts` (dev dùng `REACT_APP_API_BASE_URL=/api` tương đối → đi qua proxy Vite). `PORT` trong `frontend/.env.development.local` là tàn dư CRA, VÔ HIỆU (vite không đọc process.env.PORT) — dùng `npm run dev -- --port <n>`. Toàn bộ: `docs/development.md`.
[2026-07-29 03:35] agent-1 | KHÁC | CI sau PR#17: fix tsconfig làm bước bootstrap DB qua được → nhóm `*.real.test.ts` LẦN ĐẦU thực sự chạy trên CI và authController.real.test.ts đỏ (lockout rò rỉ giữa các test). Là nợ cũ chưa từng lộ, KHÔNG do PR#17 (diff không chạm file auth/session/token nào). Chưa sửa.
[2026-07-29 09:10] agent-2 · PR #20 gỡ 15 exhaustive-deps. GOTCHA: hook khai báo SAU useEffect dùng nó KHÔNG thể chỉ thêm vào mảng deps — deps tính lúc render nên vướng TDZ; phải dời định nghĩa lên trước effect (vd saveLayoutChart ở OrganizationPage). Hàm/hằng THUẦN thì nâng lên module scope, gỡ warning mà không cần useCallback. Trước khi thêm dep phải kiểm tra không useEffect nào phụ thuộc callback đó, kẻo sinh vòng lặp fetch.
[2026-07-29 09:22] agent-2 · PR #21 thêm 17 test component đầu tiên (DocumentsPage, ComputerConfigsPage) khoá hợp đồng list+pagination ở tầng UI. GOTCHA: hạ tầng RTL+jsdom ĐÃ có sẵn (vitest.config.ts + src/__tests__/vitest.setup.ts), chỉ là chưa ai dùng — đừng dựng lại. Mẫu: mock apiService/useAuth + stub modal nặng, GIỮ THẬT Pagination/Notification. Fixture ngày phải dùng giờ địa phương (không hậu tố Z) kẻo lệch múi giờ CI. Luôn kiểm chứng test không rỗng nghĩa bằng cách cho mock trả mảng trần rồi xác nhận test ĐỎ.
[2026-07-29 09:47] CI *.real.test.ts đỏ = 2 nguyên nhân: (1) trong test env.ts nạp root/.env TRƯỚC backend/.env mà không override -> test thừa hưởng host+creds Neon PRODUCTION, chỉ DB_DATABASE bị ép về ubnd_test; (2) audit_logs.user_id FK ON DELETE SET NULL xung đột trigger append-only 017 -> hard-delete user đã login luôn ném lỗi, afterEach văng, user+lockout tồn đọng. Fix: đảo thứ tự nạp env trong test + guard chặn DB_HOST không phải localhost; deleteTestUser chuyển soft-delete. Nhánh fix/test-db-isolation-and-user-cleanup.
[2026-07-29 09:57] UI: token .btn-* từng khai báo TRÙNG ở index.css + plugin tailwind.config.js (padding/màu dark lệch) → nay CHỈ index.css @layer components, tailwind.config.js plugins:[]. Câu tuyên truyền nằm ở Header, tiêu đề trang dùng components/layout/PageHeader.tsx — đừng render lại PageMotivation trong trang con.
[2026-07-29 09:57] Gotcha CSS: comment trong index.css chứa chuỗi '*/' (vd viết .btn-*/.input-style) sẽ ĐÓNG SỚM block comment → PostCSS 'Unknown word'. type-check + vitest KHÔNG bắt được, chỉ 'npm run build' bắt. Luôn chạy build sau khi sửa index.css.
[2026-07-29 10:17] agent-2 HOÀN TẤT: PR #23 merged vào main (451f2ff) → production https://www.xanuicam.vn đã live (xác minh assets/index-n8bp33_T.css chứa nav-link/page-title/nudgeUp/90dvh). CI trên main sau merge đỏ ĐÚNG 3 suite backend real-test như trước merge (11 test, không đổi) — nợ cũ, không do PR này.
[2026-07-29 10:57] agent-1 HOÀN TẤT: PR #22 merged (882deb4) -> CI trên main XANH TOÀN BỘ lần đầu (Test Suite + Quality + Build Application). Trước đó Build Application luôn bị SKIP vì Test Suite đỏ, nghĩa là main chưa từng được CI kiểm chứng build dù push main = auto-deploy production. Sau merge: production verified (xanuicam.vn 200, /api/health 200, /api/csrf-token trả token). Coverage gate lộ ra sau khi test hết đỏ: controllers branches 67.8% < 68% -> đã bổ sung test THẬT (fail-closed thiếu userScope; che danh tính ẩn danh khi export Excel; ROLLBACK broadcast) thay vì hạ ngưỡng -> 68.6%. CÒN LẠI: PR #21 đã rebase sạch lên main mới + 68 test frontend xanh, nhưng force-push bị chặn quyền -> cần người dùng đẩy giúp.
[2026-07-29 14:43] Dark-mode: đã quét sạch 387 chỗ thiếu biến thể dark: ở 51 file (PR #27) bằng ánh xạ bg-white→dark:bg-slate-800, text-slate-700→dark:text-slate-300, v.v. LOẠI TRỪ 2 file LUÔN SÁNG có chủ đích: LoginPage.tsx và LeadershipSchedulePage.tsx (mô phỏng giấy Word, in ra phải trắng) — đừng 'sửa' 2 file này. Còn lại: biến thể hover: ở chế độ tối vẫn loé sáng.
[2026-07-29 14:43] CI actions: setup-node v7, upload-artifact v7, download-artifact v8, github-script v9 (PR #26, đã kiểm chứng xanh). KHÔNG nâng slackapi/slack-github-action v1→v4 (PR #2 vẫn mở): từ v2 đổi sang mô hình method: của Slack API mà deploy-staging.yml đang truyền payload: JSON thô kiểu v1 — nâng mà không viết lại step sẽ gãy.
[2026-07-29 17:25] agent-1 | QUYẾT ĐỊNH | Migration 019 GỠ 6 FK của audit_logs (không đổi NO ACTION). Lý do kiểm chứng bằng chạy thật: FK ON DELETE SET NULL sinh UPDATE ngầm, bị trigger append-only 017 chặn (P0001) => purgeDeleted/deleteDeleted user LUÔN hỏng trên production (user nào cũng có audit log đăng nhập). NO ACTION sẽ chặn vĩnh viễn việc xoá user. Gỡ FK: xoá được, chain_hash + user_id lịch sử giữ nguyên. Validation ở db/migrations/validations/019_*.sql.
[2026-07-29 17:25] agent-1 | GOTCHA | auditLogController.deleteLogsAndTargets TRƯỚC ĐÂY hard-delete luôn user/role/department/article/task mà log nhắc tới (hộp xác nhận frontend không hề báo). Nó 'an toàn nhờ hỏng' do trigger 017 chặn. Sửa FK mà giữ nguyên đoạn đó = MỞ KHOÁ phá dữ liệu thật. Đã gỡ hẳn cascade, đổi tên thành deleteLogs (chỉ soft-delete dòng log) + test hồi quy chốt việc KHÔNG phát DELETE lên bảng nghiệp vụ.
[2026-07-29 17:25] agent-1 | GOTCHA | migrate.ts ABORT khi checksum lệch, mà commit 174836b (26/07) sửa COMMENT trong migration 004 và 016 đã áp dụng => db:migrate chết. render.yaml dùng 'db:migrate && npm start' nên migrate hỏng = backend KHÔNG BOOT. DB local đã đồng bộ lại checksum. CẦN KIỂM TRA Neon: nếu schema_migrations ở đó ghi checksum CŨ của 004/016 thì mọi deploy đang hỏng và 019-022 không lên được.
[2026-07-29 19:23] agent-1 | GOTCHA | scripts/hooks/pre-commit HỎNG CÚ PHÁP từ đầu (bash -n fail): 4 mẫu regex viết '...["\']' — bash KHÔNG cho escape nháy đơn trong chuỗi nháy đơn nên chuỗi đóng sớm. Hook quét secret CHƯA BAO GIỜ chạy được, và clone này còn chưa cài hook. Đã sửa sang chuỗi nháy kép, chạy install-hooks.sh, kiểm chứng thật: chặn đúng token ghp_*.
[2026-07-29 19:23] agent-1 | QUYẾT ĐỊNH | Thêm 'npm run db:migrate:repair' (xem trước; --yes mới ghi) để thoát bế tắc checksum lệch mà KHÔNG chạy SQL. Kèm chốt chặn pre-commit: cấm sửa file migration đã commit ở HEAD (override ALLOW_MIGRATION_EDIT=1). Đã kiểm chứng 4 kịch bản: chặn sửa migration cũ / override qua / thêm migration mới qua / bắt secret thật.
[2026-07-29 19:23] agent-1 | KIỂM CHỨNG | Dựng DB trắng: database.sql + 21 migration (002→022) chạy sạch; validation 019 VALID cả 4 check; 4 rollback (019-022) trả đúng nguyên trạng (schema kiosk/games=0, FK audit_logs=6). Backend 1085 test/101 suite xanh, frontend 68 test + build OK, lint 0 lỗi cả hai.
[2026-07-29 20:39] agent-1 | QUYẾT ĐỊNH | GĐ3 nạp dữ liệu: đọc SQLite bằng python3 stdlib (Node 20 chưa có node:sqlite, không thêm dep native cho việc chạy 1 lần). Mã hoá 9 cột cá nhân bằng utils/encryption.ts. 3 phát hiện buộc sửa migration 020: (1) procedures.category là taxonomy RIÊNG 79 giá trị, KHÔNG phải danh mục quầy 10 giá trị — tách bảng procedure_categories; (2) field_type thật là 'document_option' cả 46/46 dòng chứ không phải 'dropdown' như comment SQLite ghi; (3) transferred_from lưu TÊN QUẦY nguồn chứ không phải mã phiếu. Số dòng khớp DoD: 1195/400/46.
[2026-07-29 20:39] agent-1 | GOTCHA | Thứ tự sắp xếp danh mục quầy PHẢI theo byte (SQLite BINARY collation = JS sort mặc định), KHÔNG dùng localeCompare('vi') — vì counter_number = index+1. Dùng localeCompare làm lệch 1162/1195 dòng ('Nội vụ' vs 'Nông nghiệp' đảo chỗ). Sau khi sửa: phiếu tháng gần nhất khớp 24/26; các tháng cũ lệch là ĐÚNG (counter_number là giá trị lịch sử, mỗi quầy từng dùng nhiều số khác nhau).
[2026-07-29 20:39] agent-1 | QUYẾT ĐỊNH | GĐ4-5: controller kiosk trả ĐÚNG body cũ (mảng chuỗi, {data}, camelCase, số dạng chuỗi), envelope hệ thống bọc ngoài, api.ts bóc envelope — nhờ vậy 13 trang MPA không phải sửa. call_status đảo chiều serving(DB)/calling(API). Tìm kiếm làm ở tầng ứng dụng vì cột đã mã hoá (luôn lọc theo 1 ngày nên rẻ). Namespace Socket.IO /kiosk xác thực TUỲ CHỌN (màn hình sảnh ẩn danh chỉ-đọc) — không dùng io.use(verifySocketToken) của namespace gốc.
[2026-07-30 00:11] agent-1 | QUYẾT ĐỊNH | PR #29 nhánh feat/hop-nhat-kiosk-games: hợp nhất kiosk Pha 3-8, 519 file. Pha 1 (đổi schema public→core/dieuhanh) CHỦ ĐỘNG BỎ QUA vì mục tiêu chống trùng tên bảng đã đạt bằng schema riêng kiosk/games — không đáng rủi ro di chuyển 60 bảng trên production. Pha 6 gộp vào frontend/ (trang /kiosk-management) thay vì dựng app frontend-quantri thứ tư.
[2026-07-30 00:11] agent-1 | GOTCHA | Hook pre-commit sau khi sửa cú pháp mới lộ 3 lỗi nữa: (1) tự quét chính mình vì chứa danh sách mẫu regex; (2) check '.env' dùng grep KHÔNG NEO nên chặn nhầm .env.example — chính comment trong hook đã ghi đó là ngoại lệ; (3) chạy 12 lượt grep/file → 63 GIÂY với commit 500+ file, đã gộp thành 1 lượt còn 7s. Miễn trừ false positive khai ở .secrets-allowlist, mỗi dòng phải nêu lý do.
[2026-07-30 07:09] 30/07/2026 — GỠ TOÀN BỘ MODULE GAMES (quyết định sản phẩm): xoá 295 file (backend/src/modules/games, frontend-kiosk/public/games 18MB, sdk/, 12 script build game). Migration 021/022 GIỮ NGUYÊN (bất biến) — dùng migration MỚI 023_drop_games_module.sql: DROP SCHEMA games CASCADE + xoá quyền game_manage_registry. Sao lưu nguyên trạng ở ~/backup-games-20260730/ (285 file). Game sẽ làm lại thành webapp ĐỘC LẬP tích hợp bằng link — ĐỪNG thêm /games/ lại vào repo.
[2026-07-30 08:33] 30/07/2026 — BẪY DEPLOY: tsc KHÔNG xoá file đã emit, Render giữ lại thư mục dự án giữa các build ⇒ xoá file nguồn thì .js mồ côi vẫn CHẠY trên production (route games đã gỡ vẫn mount, gây 500 sau khi migration 023 xoá schema). Máy dev không tái hiện vì dist thường tạo mới. Đã sửa tận gốc ở PR #31: backend/package.json thêm 'clean' và 'build' = clean && tsc && copy:js. Khi xoá/đổi tên file nguồn, LUÔN kiểm tra dist trên production, đừng tin mỗi 'Deploy live'.
[2026-07-30 08:53] agent-1: merge PR #32 (docs dist mồ côi) vào main; XOÁ 2 nhánh remote lạc hậu feat/audit-hash-chain (857d494) + feat/auth-refresh-token-hardening (d2d51e3, 2c3ef4b) — đã đối chiếu: tính năng nằm sẵn trên main qua efaffb8 / 352cb6f+79b9786, nhánh chỉ là bản trùng từ 12/07. Gỡ luôn dòng cảnh báo tương ứng khỏi AGENTS_BOARD.md. Khôi phục (nếu cần) bằng SHA trên trong 90 ngày.
[2026-07-30 09:08] agent-1: CHỐT kiến trúc subdomain (30/07) — chọn SAME-ORIGIN mỗi subdomain (mỗi Pages project có functions/ proxy riêng), KHÔNG dùng cookie chia sẻ .xanuicam.vn. Lý do: kế hoạch cũ §5.2 (Domain=.xanuicam.vn + SameSite=lax) mở đường cho XSS ở kiosk công khai gọi api kèm phiên cán bộ — hiện không tồn tại vì FE/API same-origin qua Pages proxy. SSO làm bằng authorization-code qua id. (Redis, mã dùng-1-lần) ở pha CUỐI, giữ cookie host-only+strict. api. đổi vai thành custom domain Render mà các proxy trỏ tới; realtime gộp vào api./socket.io. Chi tiết + lộ trình 7 pha: KE_HOACH_CHUAN_HOA_SUBDOMAIN.md (thay thế §3, §5.2-5.3 của KE_HOACH_HOP_NHAT_HE_THONG.md).
[2026-07-30 15:09] agent-1 pha 1-2 subdomain (30/07): render.yaml CORS_ORIGINS mở cho 5 subdomain (KHÔNG phải mở cross-origin — trình duyệt gửi Origin kèm POST same-origin, backend phải nhận ra mới không tự chặn); FRONTEND_URL -> dieuhanh. PHÁT HIỆN LỚN: GĐ5 hợp nhất chỉ sửa api.ts, các script cấp trang của kiosk vẫn nói chuyện với backend đã xoá — hienthi/single-view gọi /api/queue/status + fetch thẳng (không bóc envelope) + io() namespace mặc định + emit join/display_room (giao thức không tồn tại); tts-service.js là code chết nên single-view rơi về /api/announcement không tồn tại. ĐÃ SỬA cả 4 lớp lỗi này + e2e-smoke.js kiểm /api/kiosk/* => test:e2e XANH 191/191 (trước là đỏ mãn tính 5 API). CÒN: dieukhien.js emit giao thức staff:* mà backend không implement — phải chuyển sang REST (/tickets/:id/call|status|transfer), Socket.IO chỉ để nhận. test:browser + test:mounted SKIP vì máy không có Chromium.
[2026-07-30 15:23] agent-1: PR #33 (nhánh feat/subdomain-chuan-hoa) — pha 1-2 chuẩn hoá subdomain. Verify bằng Chrome for Testing 151: máy KHÔNG có sudo, cài được bằng npx @puppeteer/browsers install chrome@stable + apt-get download libnss3 libnspr4 libasound2t64 rồi dpkg -x vào thư mục riêng, trỏ LD_LIBRARY_PATH. Cách này tái dùng được cho mọi phiên sau (test:browser/test:mounted của kiosk vốn SKIP vì thiếu Chromium). Kết quả: test:e2e 191/191, test:mounted 14/14, test:browser 11 lỗi so với 13 trước khi sửa.
[2026-07-30 19:54] agent-1: Pha 2 kiosk XONG phần repo. (1) dieukhien.js bỏ hẳn 5 sự kiện staff:* (không server nào implement) → hành động đi REST: gọi/hoàn tất/nhắc lại/tạm ngưng = POST tickets/:id/call với call_status calling|completed|skipped (endpoint DUY NHẤT dời con trỏ đang-gọi + phát update_display/play_announcement); chuyển phiếu giữ bắt tay 2 nhịp: nguồn client:transfer_request → đích tự POST tickets/:id/transfer rồi client:transfer_response (ghi hỏng ⇒ accepted:false). (2) update-ticket là PUT, và CỐ Ý không gửi call_status khi lưu ghi chú — đã kiểm chứng thật: PUT không có call_status thì trạng thái 'calling' giữ nguyên. (3) BÁC BỎ chẩn đoán cũ 'API trả mảng trần vs client chờ {categories:[…]}': cả admin.js lẫn dieukhien.js đều chờ MẢNG TRẦN, lỗi thật là chúng gọi fetch thẳng nên không bóc envelope → sửa ở client bằng apiRequest, KHÔNG sửa controller. (4) Backend thêm emitToControl/notifyControl: trước đây KHÔNG có gì phát server:update_control nên handler chờ nó ở dieukhien là code chết; thêm relay client:transfer_response. (5) /dieukhien trước giờ KHÔNG có gate đăng nhập dù mọi endpoint đòi quyền → nạp js/admin-auth.js, PROTECTED_SELECTOR thêm 4 nút vận hành, window.kioskAuth.goToLogin() khi 401, trang login lấy từ VITE_LOGIN_URL (mới).
[2026-07-30 23:24] agent-1: Pha 4 (quantri) — khảo sát + chốt cách thi công, CHƯA viết code. Số đo: frontend/ có 200 file TS; services/ được 60 file import, utils/ 44, components/common 28, layout 25 → cấm copy hạ tầng, phải tách packages/app-shell bằng codemod (dry-run→apply), không sửa tay. Sidebar.tsx là NGUỒN SỰ THẬT của menu và đang hardcode → phải inject menu, nếu không quantri mọc lại menu dieuhanh. HAI ĐIỂM CHẶN cần người dùng quyết: (1) tách domain = quản trị viên ĐĂNG NHẬP HAI LẦN cho tới pha 7 vì cookie host-only — không sửa được bằng cấu hình; nếu không chấp nhận thì phải làm pha 7 TRƯỚC pha 4; (2) /account thuộc app nào (vừa là danh bạ hằng ngày vừa là nơi tạo/khoá tài khoản). Chi tiết + bảng chia trang: KE_HOACH_CHUAN_HOA_SUBDOMAIN.md mục Pha 4.
[2026-07-30 23:42] Pha 4 CHỐT (30/07, người dùng quyết): (1) CHẤP NHẬN đăng nhập 2 lần tới Pha 7 ⇒ giữ thứ tự 4→5→6→7, KHÔNG đảo Pha 7 lên trước; (2) /account chuyển sang quantri., dieuhanh. giữ lại trang danh bạ CHỈ-ĐỌC.
[2026-07-31 00:03] Pha 4a XONG (31/07): tách 92 file sang packages/app-shell, codemod 274 specifier/65 file. Bẫy đã gặp: (1) Tailwind content PHẢI thêm ../packages/app-shell/src (thiếu = purge âm thầm, build vẫn xanh); (2) app-shell KHÔNG có node_modules riêng ⇒ tsconfig paths '*' → ../node_modules + ../node_modules/@types, Vite cần plugin shellResolver, vitest cần server.fs.allow; (3) codemod regex sót vi.mock() và import side-effect ('./index.css'); (4) chỉ ghi lại specifier phân giải được ra file thật để bỏ qua import trong template literal.
[2026-07-31 00:27] Pha 4b XONG (31/07): dựng frontend-quantri (cổng 3002, 30 route quản trị, menu+test riêng). QUYẾT ĐỊNH: pha 4b KHÔNG đụng frontend/ (app đang chạy thật) — trang quản trị nạp qua alias CẦU TẠM @dieuhanh, pha 4c phải chuyển file sang frontend-quantri/src/pages/ rồi xoá alias ở 4 chỗ (vite/vitest/tsconfig/tailwind) + rút gọn build command Pages. Bẫy: package.json quantri KHÔNG được có type:module (postcss/tailwind là CommonJS); quantri-env.d.ts KHÔNG được có export cấp cao nhất (thành module ⇒ declare module '*.png' hoá augmentation ⇒ TS2307 mọi import ảnh).
[2026-07-31 00:33] 31/07: lấp khoảng trống lint app-shell — gói có eslint.config.mjs + devDeps lint riêng (0 lỗi/223 cảnh báo). Không dùng chung config của frontend được vì flat config từ chối file ngoài base path.
[2026-07-31 00:47] Pha 5 backend XONG (31/07): module cms (migration 024 + repository/controller/routes + 45 test, coverage module 82,4%; backend 1181/1181, global 88,89% stmts). Thiết kế: KHÔNG workflow duyệt nhiều bước — điểm kiểm soát duy nhất là tách content_manage (soạn) khỏi content_publish (đăng), nên đổi trạng thái có endpoint RIÊNG và PUT /posts/:id TỪ CHỐI trường status. Endpoint công khai cưỡng bức publishedOnly ở controller (tin ?status của client = lộ bản nháp ra internet). Schema cms dùng deleted_at đơn giản như kiosk, KHÔNG phải VIEW *_all — đừng áp resolveBaseTable().
[2026-07-31 01:52] Pha 5 (CMS UI+ảnh R2), Pha 6 (cổng thông tin www + Pages Functions SEO/sitemap), Pha 7 (SSO authorization-code, MẶC ĐỊNH TẮT) đã xong phần repo, commit d6a765e/2ca9374/19f7d3d trên feat/subdomain-chuan-hoa. CHƯA push.
[2026-07-31 01:53] Bẫy đã trả giá: cột VARCHAR + tham số dùng ở cả vế gán lẫn vế so sánh trong CÙNG câu SQL => Postgres 'inconsistent types deduced for parameter $1', phải ép ::text ở CẢ HAI vế. Unit test mock db.query KHÔNG bắt được — cần *.real.test.ts. Lỗi này làm PATCH /cms/posts/:id/status trả 500 trên mọi lần xuất bản.
[2026-07-31 02:47] Pha 4c XONG: alias @dieuhanh đã xoá khỏi repo. Codemod chuyển 54 file — 16 sang packages/app-shell/src (LoginPage, SettingsPage+settings/*, DeletedRecordsPage, 4 trang trạng thái, AnnouncementModal, TaskDetailModal, EditTaskModal), 35 sang frontend-quantri/src, xoá 2 trang chết RolesPage/PositionsPage.
[2026-07-31 02:47] Bài học 4c: file vào trong app-shell KHÔNG được dùng alias '@shell/...' (lint:layering chặn tự-tham-chiếu) ⇒ phải đổi sang đường dẫn tương đối theo vị trí MỚI. 13/16 file phải sửa import chỉ vì lý do này.
[2026-07-31 02:47] Tiền lệ 4c: app-shell nay chứa được modal NGHIỆP VỤ (TaskDetailModal/EditTaskModal) khi HAI app thật sự cùng render — dieuhanh (TasksPage/Dashboard/Reports) + quantri (AccountAndPermissionsPage). Để ở app nào cũng là phụ thuộc ngược; sao chép là đúng bẫy pha 4a dựng shell để tránh.
[2026-07-31 02:47] Cloudflare _redirects: '/account/*' KHÔNG khớp '/account' — phải viết HAI dòng cho mỗi nhóm. Mọi luật 301 phải nằm TRÊN dòng SPA fallback '/* /index.html 200' vì luật trúng đầu tiên thắng. frontend/src/navigation/QuantriRedirect.test.tsx khoá _redirects và App.tsx phải cùng một tập đường dẫn.
[2026-07-31 03:43] Pha 7 mục (c)+(d) XONG: kiosk có trang hạ cánh SSO /auth/ (frontend-kiosk/public/auth/ + public/js/sso.ts, bản MPA của lib/sso.ts); LoginPage của app-shell khi ssoEnabled() thì beginSso() thay vì vẽ form mật khẩu.
[2026-07-31 03:43] Kiosk MPA: bộ dò trang của Vite chỉ nhận thư mục MỘT cấp dưới public/ ⇒ redirect_uri của kiosk là /auth/ chứ KHÔNG phải /auth/callback như hai app React.
[2026-07-31 03:43] Bẫy: trang hạ cánh SSO của kiosk nếu tự chuyển sang /admin/ khi SSO chưa bật sẽ kéo theo toàn bộ 401 của trang đó — test:browser vọt từ 8 lên 15 lỗi. Trang trung chuyển phải ĐỨNG YÊN khi không có việc để làm.
[2026-07-31 08:41] 31/07 agent-1: PR #34 mở cho 3 commit cuối (Pha 4c + 2 commit Pha 7) — PR #33 đã MERGED nên main CHỈ thiếu 3 commit này, không phải 16 như local main (đã lỗi thời 14 commit) gợi ý. Luôn 'git fetch' rồi so với origin/main, đừng tin local main.
[2026-07-31 08:41] 31/07 agent-1: dark-mode codemod 218 class/65 file. HAI BẪY đã trả giá: (1) literal có nội suy ${...} — class cần vá thường nằm trong nhánh ternary, nối dark: vào cuối literal thì nó áp cho CẢ HAI nhánh, nút không-active cũng sáng lên ⇒ script phải BỎ QUA mọi literal chứa ${; (2) types/modal-template.ts là template literal chứa nguyên khối mã React, script nối class vào sau 'export default YourModal;' ⇒ bỏ qua literal trải > 3 dòng. Nền cấp trang (min-h-screen) phải dark:bg-slate-900, không phải tông panel slate-700.
[2026-07-31 08:41] 31/07 agent-1: kiosk có BỐN chỗ gọi endpoint không tồn tại, không phải một. Đã sửa: /admin nút Xuất báo cáo dùng path CŨ /api/export-excel (route thật /api/kiosk/export-excel); nút Nhập Excel GỠ HẲN (người dùng chốt — phiếu chứa cột cá nhân mã hoá, đường nạp chính thức là npm run kiosk:load); /general 'export-excel-general' chuyển sang dựng file .xls ngay tại trình duyệt bằng hàm convertToStyledExcelHtml vốn có sẵn mà chưa ai gọi. CÒN TREO: apiUrl('analytics') cũng không có route — bị .catch() nuốt nên không hại người dùng, nhưng mỗi trackEvent ghi một lỗi 500 ra console. Cần quyết định có thu thập analytics kiosk hay không (dữ liệu sử dụng của công dân).
[2026-07-31 09:02] 31/07 agent-1: CI 'Quality' đỏ từ commit 16a80cd (KHÔNG phải do 2 commit dark-mode/kiosk). @testing-library/react@16 khai @testing-library/dom là peerDependency; CI cài frontend bằng 'npm install --legacy-peer-deps' — cờ này KHÔNG cài peer ⇒ mất 'screen' ⇒ TS2305. Máy dev xanh vì npm install thường tự cài peer. ĐÃ CHỨNG MINH bằng cài đối chứng: package.json cũ + --legacy-peer-deps = 1225 gói, KHÔNG có @testing-library/dom; bản mới = 1227 gói, CÓ 10.4.1. Đã khai tường minh ở frontend + frontend-quantri, và thêm scripts/check-peer-deps.mjs (allowlist kèm lý do cho playwright/playwright-core/quill-delta/zod) chạy trong job quality SAU mọi bước install.
[2026-07-31 09:07] 31/07 agent-1: frontend-quantri VÀO CI cả 3 job (quality: install+lint+typecheck; test: 19 test hard-fail; build: bắt Tailwind purge âm thầm). Cài bằng npm install THƯỜNG — đã kiểm chứng bằng cài thật thư mục sạch, không cần --legacy-peer-deps như frontend. Đặt bước cài quantri TRƯỚC bước check-peer-deps nên hàng rào peer phủ luôn nó. PHÁT HIỆN CÒN TREO: bước 'Run frontend tests' vẫn continue-on-error:true kèm comment 'Frontend chưa có test file (gap R7)' — comment ĐÃ LỖI THỜI, frontend nay có 83 test thật, nghĩa là test frontend hỏng KHÔNG làm đỏ CI.
[2026-07-31 09:10] 31/07 agent-1: bước 'Run frontend tests' của CI HỎNG SẴN chứ không chỉ là continue-on-error — nó chạy 'npm test -- --coverage --watchAll=false --passWithNoTests', cờ kiểu Jest, mà frontend đã sang Vitest ⇒ CACError: Unknown option --watchAll ⇒ thoát 1 ngay. continue-on-error nuốt mất, nên 83 test frontend CHƯA TỪNG chạy trong CI. Đã đổi sang 'npm run test:coverage' + bỏ continue-on-error. Kèm theo: thêm reporter json-summary vào frontend/vitest.config.ts vì bước 'Comment coverage on PR' đọc coverage-summary.json mà bộ mặc định của vitest không sinh ra file đó (nên nó luôn báo n/a). Frontend coverage thật = 3,42% dòng; KHÔNG có ngưỡng cấu hình nên không làm đỏ CI.
[2026-07-31 09:17] 31/07 agent-1: PR #34 XANH HOÀN TOÀN trên 611e4c5 (chỉ còn 'label' đỏ = nợ cũ). Đã xác minh TỪNG BƯỚC chứ không tin mỗi 'Test Suite pass' — vì trước đây job này pass trong khi test frontend âm thầm hỏng. 6 commit: 3 của Pha 4c/7 + dark-mode + kiosk + 3 commit CI.
[2026-07-31 09:47] 31/07 agent-1: BẪY biến môi trường superadmin — backend/.env có CẢ SUPERADMIN_USERNAME/SUPERADMIN_PASSWORD lẫn SUPERADMIN_ADMIN_PASSWORD, nhưng scripts/bootstrap-superadmins.ts CHỈ đọc SUPERADMIN_<TEN>_PASSWORD (ADMIN/UBND/DANGUY). Đăng nhập bằng SUPERADMIN_PASSWORD sẽ 401 dù bootstrap báo thành công. Đúng: identifier='admin' + SUPERADMIN_ADMIN_PASSWORD (đã kiểm chứng: 200 + /auth/me trả admin). Cẩn thận khoá luỹ tiến sau 5 lần sai.
[2026-07-31 11:56] 31/07 agent-1: DỰNG XONG HẠ TẦNG THẬT (qua Cloudflare/Render API, không phải dashboard). 6 subdomain sống: api/dieuhanh/quantri/kiosk/id/portal; www→301 dieuhanh (giữ path+query); apex→301 portal; SSO ĐÃ BẬT cả backend lẫn 3 app. Bài học: (1) Pages project nối Git TẠO ĐƯỢC qua API (source.config cần owner_id+repo_id, chép từ project sẵn có) — không cần bấm tay; (2) đổi env var trên Render KHÔNG tự nạp lại, phải POST /deploys rồi xác nhận uptime tụt về vài chục giây; (3) phép thử phân biệt SSO bật/tắt: POST /api/sso/token KÈM CSRF → 404=tắt, 400=bật (không kèm CSRF thì 403 ở cả hai, vô nghĩa); (4) tạo Redirect Rule trỏ sang hostname CHƯA tồn tại = sự cố thật, apex từng NXDOMAIN vì rule trỏ portal. trước khi portal. có DNS — dựng đích TRƯỚC, rule SAU; (5) _routes.json ĐÃ kiểm chứng thật: /socket.io/socket.io.js trả 155.836 byte = client thật, Function thắng file tĩnh.
[2026-07-31 11:56] 31/07 agent-1: LỆCH CÓ CHỦ ĐÍCH so với kế hoạch — cổng thông tin ở portal.xanuicam.vn chứ KHÔNG phải www.+apex. Kế hoạch pha 6 định cho www. thành cổng thông tin và gỡ 301 của pha 3, nhưng làm vậy giết mọi bookmark www./tasks của cán bộ. Cách đang chạy giữ cả hai: www.→301 dieuhanh. cứu bookmark, apex→301 portal. cho người dân. ĐỪNG sửa lại cho khớp kế hoạch.
[2026-07-31 12:07] QUYẾT ĐỊNH (agent-1, 31/07): KHÔNG thu analytics kiosk. Gỡ hẳn fetch POST apiUrl('analytics') trong general.js (route backend chưa từng tồn tại → mỗi trackEvent đẻ 1 lỗi 500 bị .catch() nuốt). trackEvent giữ lại nhưng chỉ log console ở localhost; thống kê thật (menu_visit_count, menu_visits_by_date) vốn đã nằm ở localStorage nên KHÔNG mất gì. Lý do không dựng route: không ai tiêu thụ dữ liệu, và thu hành vi người dân ở kiosk công kèm userAgent+URL là quyết định sản phẩm/pháp lý chứ không phải dọn code. Cần thật thì dựng route + bảng có chủ đích.
[2026-07-31 12:07] BẪY TEST (agent-1, 31/07): frontend-kiosk 'npm run test:e2e' báo 166/171 asset hỏng ('served as text/html') KHÔNG phải regression — đó là VA CHẠM CỔNG. e2e-smoke.js spawn server tĩnh ở PORT (mặc định 8005) phục vụ dist/; nếu đã có vite dev của kiosk giữ :8005 thì server test không bind được, mọi request rơi vào server cũ phục vụ public/ (không có asset băm) → SPA fallback trả index.html. Chẩn: ss -ltnp | grep 8005. Cách chạy: PORT=8016 npm run test:e2e. ĐỪNG kill vite của agent khác.
[2026-07-31 12:38] CI (agent-1, 31/07): check 'label' ĐÃ VÁ và merged (PR #38). Gốc: repo đặt default_workflow_permissions=read nên GITHUB_TOKEN không có quyền pull_requests → pulls.get 403. Vá 3 lớp: khai permissions: pull-requests: write; đọc PR từ context.payload thay vì gọi pulls.get (bỏ hẳn lượt API 403); bỏ qua PR từ fork (fork luôn token chỉ-đọc). TỪ NAY 'label' đỏ = lỗi thật. BẪY gặp phải khi vá: viết literal dấu nội suy ${{ }} trong CHÚ THÍCH của khối script làm GitHub từ chối cả file — run hiện dưới event push, chết 0s, không job, check BIẾN MẤT thay vì đỏ; yaml.safe_load vẫn báo hợp lệ.
[2026-07-31 13:14] kiosk lint:js hỏng do CHÍNH overrides trong frontend-kiosk/package.json: 'brace-expansion': '^5.0.8' ép v5 (export {expand}) vào minimatch@3.1.5 vốn require() nó như HÀM -> 'expand is not a function', ESLint chết sạch. Vá: thêm override LỒNG có phạm vi 'minimatch@3': {'brace-expansion': '^1.1.18'}, giữ v5 cho phần còn lại. KHÔNG gỡ được advisory GHSA-mh99-v99m-4gvg (range <=5.0.7 phủ cả 1.x, chỉ >=5.0.8 vá) vì eslint-plugin-react BẢN MỚI NHẤT 7.37.5 vẫn ghim minimatch ^3.1.2 và neostandard 0.13 peer eslint ^9 => 'npm audit fix --force' vừa phá neostandard vừa KHÔNG loại được minimatch@3. Rủi ro chấp nhận: devDependency, DoS trên glob do repo tự kiểm soát, không chạy ở production. Xem lại khi eslint-plugin-react bỏ minimatch 3.
[2026-07-31 13:34] Chuẩn hoá giao diện 31/07 (agent-2): (1) em dash '—'→'-' toàn bộ 69 file frontend, trừ socket.io stub + baseline JSON + file sinh ra. (2) Màu nền dùng chung: token --app-bg=#f1f5f9 khai ở app-shell/index.css VÀ kiosk theme.css; kiosk đặt ở 'html' (không phải body) để hoa văn trống đồng vẫn hiện; thêm fallback ở ui-system.css cho /auth và /api/export-excel vốn không nạp theme.css. Đổi tông nền = sửa 3 chỗ đó. (3) Bộ nhận diện: nguồn duy nhất assets/brand/ + scripts/build-brand-assets.mjs (sinh, cần sharp ở frontend-kiosk/node_modules) + scripts/sync-brand-assets.mjs (phát vào public/ 3 app; favicon BẮT BUỘC copy vì Vite chỉ phục vụ public/ của chính app). (4) GỐC RỄ lỗi :3002: LoginPage trỏ URL tuyệt đối /background-login.jpg chỉ có ở frontend/public → 404; và quantri/index.html thiếu link Google Fonts nên font rơi về hệ thống. Nay ảnh đi qua import trong app-shell nên mọi app dùng shell tự có.
[2026-07-31 14:08] GOTCHA GIT (agent-1, 31/07): các agent dùng chung THƯ MỤC nên dùng chung luôn GIT INDEX. Quy ước 'chỉ git add file cụ thể, không add -A' KHÔNG đủ để phòng: nếu agent khác vừa 'git add' phần của họ, 'git commit' của bạn ghi TOÀN BỘ index chứ không riêng file bạn add — commit ec9ad83 phiên này đã cuốn nhầm 5 file xoá của agent-2 dù đã add đúng 4 file. Cách phòng đúng: dùng 'git commit -- <đường dẫn>' hoặc kiểm tra 'git diff --cached --name-only' NGAY TRƯỚC khi commit; tốt nhất là hai agent không commit chồng lấn thời điểm.
[2026-07-31 14:32] Chuẩn hoá giao diện vòng 2 (agent-2): (1) frontend-portal (:3003) + frontend-id (:3004) BỊ BỎ SÓT ở vòng 1 — nay đã vào bộ nhận diện chung, khai trong APPS + PLAIN_HTML_APPS của sync-brand-assets.mjs. Hai app này là HTML THUẦN nên logo phải là file tĩnh trong public/ + <picture> srcset, không import qua bundler được. (2) Vòng tròn chữ 'NC' tạm ở header portal và trang id ĐÃ THAY bằng logo thật. (3) Gộp nốt en dash '–' (ký tự KHÁC em dash, bộ thay thế cũ không bắt). (4) Tô sáng tìm kiếm ở /thu-tuc: highlightMatches() trong procedures.ts — ô tìm kiếm bỏ dấu nên phải chuẩn hoá TỪNG ký tự + ánh xạ ngược chỉ số về chuỗi gốc; normalizeChar BẮT BUỘC khớp normalize (trừ trim), lệch là tô sai. (5) test:mounted kiosk KHÔNG CÒN SKIP: Chrome đã có sẵn ở frontend-kiosk/chrome/, chỉ cần dpkg -x 3 lib vào .tools/libs rồi LD_LIBRARY_PATH=$PWD/.tools/libs/usr/lib/x86_64-linux-gnu — kết quả 15/15 PASSED, test:browser đúng mức nền 8 lỗi 401. (6) TỒN ĐỌNG: bản ghi kiosk.procedures id 82 trong DB vẫn còn '–' (bản JSON tĩnh đã sửa).
[2026-07-31 16:14] PR #40 ĐÃ MERGE 31/07 09:03Z (merge commit 203558e), 6/6 check xanh. ĐÃ KIỂM CHỨNG TRÊN PRODUCTION: favicon mới khớp checksum ở CẢ 5 subdomain; ảnh nền đăng nhập quantri trả 200 image/webp 331KB (hết khối xám); HTML quantri có link Google Fonts; bundle thu-tuc trên portal có class 'tim-kiem'; chữ NC đã biến mất ở portal+id. BẪY DEPLOY: 5 Pages project deploy ĐỘC LẬP, ubnd-quantri chậm hơn ~5 phút vì lệnh build dài nhất (shared-types + app-shell + quantri) — thấy asset cũ ngay sau merge là BÌNH THƯỜNG, chờ rồi kiểm lại bằng '?cb=random' để bỏ qua cache CDN trước khi kết luận deploy hỏng. TỒN ĐỌNG chưa xử lý: (1) bản ghi kiosk.procedures id 82 trong DB còn dấu en dash; (2) nền chế độ sáng của frontend-portal là #ffffff chứ không phải #f1f5f9 dùng chung.
[2026-07-31 16:23] Nền portal (agent-2, 31/07): TỒN ĐỌNG 'portal nền #ffffff' ĐÃ XONG (commit 6f18857). Cách làm quan trọng cho lần sau: frontend-portal dùng CSS thuần, token --nen của nó vừa là nền TRANG vừa là màu BỀ MẶT (thẻ tin, ô nhập, nút phân trang) - đổi thẳng --nen sang #f1f5f9 là sai, thẻ tin xám theo và mất tương phản. Đúng: thêm token RIÊNG --app-bg (sáng #f1f5f9 / tối #0f172a), --nen giữ trắng. Nay màu nền dùng chung khai ở BA nơi, đổi tông nền phải sửa cả ba: packages/app-shell/src/index.css, frontend-kiosk/public/css/theme.css, frontend-portal/src/styles.css. Hệ quả bắt buộc: .quick a và .notice của portal đang dùng --nen-phu (#f8fafc) gần trùng nền mới nên đã chuyển sang --nen; chân trang giữ --nen-phu vì có viền đỏ 3px phân định. Kiểm tra: build+type-check+lint(0 error)+test 50/50 xanh.
[2026-07-31 16:34] LỖI AUTH cần agent-1 sửa (phát hiện 31/07 khi bật SSO local): authRepository.getUpdatedUserForPayload (repositories/authRepository.ts:63-80) KHÔNG select cột token_version ⇒ buildUserPayload (controllers/authController.ts:117) mint JWT với tokenVersion=0, trong khi createLoginSession (utils/sessionManager.ts:132) ghi giá trị THẬT từ DB vào sessions.token_version ⇒ authMiddleware.ts:52-58 so lệch, ném 'Token version has been revoked' → 401. Dính MỌI user từng đổi mật khẩu (token_version>0), ở CẢ HAI đường mint token: POST /api/auth/refresh (authController.ts:1107) và SSO đổi mã (modules/sso/ssoController.ts:179). Đo thật với admin (token_version=1): login→/auth/me 200, refresh 200, /auth/me sau refresh 401; SSO đổi mã 200 rồi /auth/me 401. Hệ quả trình duyệt: hết hạn access token sau ~30 phút, axios refresh single-flight thành công nhưng vẫn 401 → đá về trang đăng nhập. SSO ĐANG BẬT ở production nên KHÔNG chỉ là lỗi local. Sửa: thêm u.token_version vào SELECT của getUpdatedUserForPayload (vá cả hai đường); cần test hồi quy vì CI có ngưỡng coverage 70%. Người dùng đã giao việc này cho agent-1.
[2026-07-31 16:35] agent-1 · bảo mật file-preview: GỠ mount tĩnh công khai /preview-cache (đứng TRƯỚC verifyTokenOptional -> ai có URL cũng tải được bản PDF của tài liệu riêng tư, kể cả uploads/imports chứa dữ liệu công dân mà app.ts cố ý 404). Nay PDF phát qua /api/files/private/:token với root='preview', inline=true, gắn uid. ĐỪNG dựng lại mount đó. Kèm: getPreview đổi sang resolveWithinRoot (startsWith trần cho lọt thư mục anh em trùng tiền tố tên, vd uploads-backup/), chặn nhánh imports/, bỏ process.cwd(). Phụ: /preview-cache vốn KHÔNG nằm trong proxy Pages Functions nên xem trước Office đang hỏng trên production - chuyển sang /api/... sửa luôn.
[2026-07-31 16:37] 31/07 agent-2: vá nốt dark-mode trong literal có nội suy — 61 class/14 file (frontend, quantri, app-shell). Cách làm: tách "đơn vị class" = từng chuỗi trong nhánh ternary (KHÔNG phải cả literal) rồi mới so thiếu, nên nhánh active/không-active nhận dark riêng, không lây chéo. HAI bẫy đã trả giá: (1) bản đồ ánh xạ HỌC MÁY từ cặp "X dark:Y" liền kề bị nhiễu — vd suy ra border-blue-500 -> dark:border-slate-600, thực chất là border-gray-300 ... dark:border-slate-600 với focus:border-blue-500 xen giữa; phải chốt TAY bản đồ, mỗi dòng kèm tiền lệ đếm được. (2) sai dương khi dark: đã nằm NGOÀI nội suy (áp vô điều kiện): DataRepoPage, DashboardPage, Pagination, RoleModal, DayMeetingsModal — kiểm tra dark: theo họ+biến thể ĐÃ CHUẨN HOÁ thứ tự (dark:hover: === hover:dark:) mới loại được. Quy ước repo: dark:hover: (56 lần), hover:dark: 0 lần. Bonus: ProfileSettingsPage dùng toán tử && trong template literal nên khi bật sửa sẽ chèn class rác "false"; đã đổi sang ternary và đưa dark:bg-slate-700 vào đúng nhánh disabled (utility bg-slate-100 THẮNG dark:bg-slate-700 của .input-style vì khác layer).
[2026-07-31 16:46] 31/07 agent-2 → agent-1 (BÀN GIAO PUSH): agent-2 đã xong dark-mode nội suy, commit 593f5f8 trên nhánh feat/chuan-hoa-giao-dien-nhan-dien, đã nhả lock, KHÔNG push. Nhánh này đang chứa CẢ commit của agent-1 lẫn agent-2 (thư mục làm việc dùng chung một HEAD). CHỐT: agent-1 làm xong phần bảo mật file-preview (path containment + gỡ mount tĩnh công khai /preview-cache) thì PUSH MỘT LƯỢT cho cả hai phần, agent-2 không push riêng. Trước khi push nhớ: build @ubnd/shared-types → type-check + lint sạch ở workspace backend, và xin xác nhận người dùng theo cổng an toàn git trong CLAUDE.md.
[2026-07-31 18:33] agent-1 · VÁ LỖI AUTH bàn giao (dòng 83): authRepository.getUpdatedUserForPayload thiếu u.token_version VÀ u.public_id. Hàm này nuôi buildUserPayload ở MỌI đường mint token trừ login (/auth/me, /auth/refresh, cập nhật hồ sơ, đổi mã SSO) — login dùng findUserForLogin vốn có đủ cột, nên lỗi chỉ hiện SAU refresh/SSO. Hệ quả 1: tokenVersion rơi về 0 trong khi sessions.token_version giữ giá trị thật => 401 'Token version has been revoked' với mọi tài khoản từng đổi mật khẩu. Hệ quả 2: publicId undefined trong MỌI phản hồi /auth/me (AuthContext hydrate từ đó). ĐO THẬT sau vá trên :8080 với admin (token_version=1): login 200 -> refresh 200 -> /auth/me SAU refresh 200 (trước là 401), publicId có giá trị. BẪY GẶP PHẢI: chú thích thêm vào giữa template literal SQL mà chứa dấu BACKTICK sẽ ĐÓNG SỚM chuỗi, cắt cụt câu lệnh từ chỗ đó (mất token_version, role_name, department, cả WHERE) — cùng họ bẫy '*/' của index.css và nội suy ${{ }} trong workflow. type-check KHÔNG bắt được; test khẳng định cột mới bắt được.
[2026-07-31 18:43] 31/07 agent-3: cherry-pick 84afa8f (PR #41, fix token_version + public_id) vào feat/chuan-hoa-giao-dien-nhan-dien = commit 6aed73a, theo yêu cầu người dùng để môi trường local test được. Lý do cần: nhánh feat KHÔNG chứa bản vá (agent-1 tách nhánh riêng từ 203558e), nên backend local chạy code hỏng — đo lại thấy /auth/me 401 sau refresh. Sau cherry-pick: refresh 200/SSO 200, type-check sạch, lint 0 error, jest 62/62 (repositories + sso). CẢNH BÁO khi merge: commit này sẽ TRÙNG với PR #41 nếu #41 merge vào main trước rồi feat rebase/merge — nếu git không tự khử trùng thì bỏ commit 6aed73a khỏi feat, giữ bản của #41.
[2026-07-31 19:37] agent-1 · 31/07 chốt phiên: MERGED #41 (auth token_version/public_id), #42 (bảo mật file-preview + IDOR), #2 (dependabot slack v1->v4), #43 (permissions cho commit-lint). BA BẪY GẶP PHẢI, ghi lại để khỏi trả giá lần nữa: (1) BACKTICK trong chú thích đặt giữa template literal SQL ĐÓNG SỚM chuỗi, cắt cụt câu lệnh từ đó (mất token_version, role_name, department, cả WHERE) — type-check KHÔNG bắt, chỉ test khẳng định cột mới bắt được; cùng họ bẫy '*/' của index.css. (2) commit merge KHÔNG theo Conventional Commits làm commitlint đỏ — commit merge do GitHub tự sinh ('Merge <sha> into <sha>') thì được bỏ qua, nhưng merge có message tự viết thì KHÔNG; muốn đồng bộ main vào nhánh thì rebase, đừng merge. (3) context 'secrets' KHÔNG dùng được trong 'if' cấp step của Actions (chỉ có ở with/env) — viết vào đó là hỏng cả workflow. Kèm: repo có 0 secret (API total_count=0) nên bước Slack trong deploy-staging.yml chưa từng gửi được gì; workflow đó cũng chỉ workflow_dispatch và không có bước deploy thật — cân nhắc gỡ hẳn nếu không định dùng Slack.
[2026-07-31 19:47] agent-1 · 31/07 tối: HOÀN TẤT cụm brand mà phiên trước bỏ dở (không rõ chủ, không lock, mtime 18:10). Nội dung: tách favicon-master.webp riêng khỏi logo-master.webp; build-brand-assets.mjs sinh luôn bộ favicon + tự đóng gói favicon.ico bằng packIco() (PNG 16/32/48 theo ICONDIRENTRY, không cần thư viện ngoài sharp); sync-brand-assets.mjs đồng bộ thêm logo kiosk GIỮ NGUYÊN tên 5tayHCC.opt.webp (12 trang HTML tĩnh trỏ vào đó); bỏ border-radius:50%+overflow:hidden ở header portal và trang id vì logo mới trong suốt bị cắt mất bên trái; 5tayHCC-LV.opt.webp cũ của kiosk được git nhận là RENAME thành favicon-master.webp (0 tham chiếu còn lại). CÁCH KIỂM CHỨNG dùng lại được cho mọi cụm sinh-tự-động bỏ dở: chạy lại build+sync --apply rồi đếm 'git status --short' — nếu số file thay đổi KHÔNG tăng thì working tree đúng bằng đầu ra của script, tức cụm đó tái lập được và an toàn để commit.
[2026-07-31 19:54] 31/07 agent-2: TỒN ĐỌNG en dash trong DB ĐÃ XONG (fc7ee22). kiosk.procedures id 82 nạp từ SQLite cũ nên mọi đợt quét mã nguồn đều trượt. Vá HAI đầu mới triệt để: migration 025 sửa dữ liệu đang có (bắt buộc là migration - dữ liệu ở Neon, mà kiosk:load cố ý chạy tay nên không có lần nạp sau nào sửa hộ; Render chạy db:migrate mỗi deploy) + normalizeDashes trong migrate-kiosk-data.ts chặn tái phát. PHẠM VI PHẢI HẸP: chỉ procedures.name/content. KHÔNG áp cho counter_categories.name (là khoá sắp xếp byte sinh ra display_order = số quầy ĐÃ IN trên phiếu cũ) và procedure_categories.name (khoá ON CONFLICT, sửa lệch sẽ đẻ danh mục trùng). Kiểm chứng thật: 0 bản ghi còn dấu, chạy lại migration = no-op, jest kiosk 81/81.
[2026-07-31 19:54] 31/07 agent-2: pipeline brand SINH RA TẤT ĐỊNH - đã kiểm 3 lần chạy liên tiếp, checksum PNG/WebP/ICO trùng khít. Nếu chạy lại --apply mà git báo ảnh đổi byte thì KHÔNG phải sharp ngẫu nhiên, mà là file trong cây làm việc do bản script CŨ sinh ra (đã gặp: logo-128/512.png lệch vì sinh trước khi thêm palette:true). Cách xử đúng: chạy lại cả hai script rồi lấy kết quả mới, đừng khôi phục file cũ.
[2026-07-31 20:19] agent-1 · 31/07 20:15 CHỐT NHÁNH: rebase feat/chuan-hoa-giao-dien-nhan-dien lên origin/main (đang sau 11 commit) rồi push force-with-lease, mở PR #44 gộp công việc của cả 3 agent. Git TỰ BỎ 6aed73a vì trùng patch với PR #41 đã merge - đúng như 'git cherry origin/main HEAD' dự báo bằng dấu '-' (dùng lệnh này TRƯỚC mọi rebase để biết commit nào sẽ biến mất, khỏi hoảng khi đếm lại thấy hụt). BÀI HỌC VẬN HÀNH: trong thư mục dùng chung, lệnh rebase KHÔNG chỉ cần index sạch mà cần cả một khoảng lặng - hai lần liên tiếp tôi phải hoãn vì agent khác bắt đầu ghi file giữa lúc chuẩn bị (19:47 migration 025, 19:53 login.ts). Cách xử đã dùng: đặt tag cứu hộ trước khi rebase, ghi lưu ý lên bảng xin khoảng lặng, và stash theo TỪNG ĐƯỜNG DẪN (git stash push -- <file>) để không cuốn việc của người khác. Cổng kiểm tra trước push: backend 1264 test + coverage 88.9/72.7 (ngưỡng 70), frontend 83 test + lint:layering, kiosk e2e 265/265, cả 5 app build xanh.
[2026-07-31 21:24] agent-1 · 31/07 21:20 HAI LỖI PRODUCTION tìm ra khi người dùng đăng nhập thật (PR #45, merge ad3554f). (1) SSO state mismatch: frontend-id/src/login.ts gọi createState() sinh state MỚI lúc xin mã, trong khi app đích lưu state của CHÍNH NÓ trong sessionStorage rồi so lại ở callback => lệch 100% => 'Phiên đăng nhập không khớp' trên CẢ dieuhanh/quantri/kiosk. id. chỉ là bên CHUYỂN TIẾP, không giữ bản sao nào để đối chiếu; backend vốn trả lại nguyên văn state (ssoController.ts:99). CÁCH CHẨN NHANH LOẠI LỖI NÀY: so state trong URL gửi sang id. với state trong URL callback - khác nhau là lỗi ở bên chuyển tiếp, giống nhau mới là lỗi lưu/so ở app đích. (2) Vân tay thiết bị gửi về MÁY NGƯỜI DÙNG: useFingerprint đọc process.env.REACT_APP_FINGERPRINT_ENDPOINT, mà vite.config.mts của frontend+quantri mặc định biến đó = ${apiBase}/fingerprint, và apiBase khi Pages build thiếu env = http://localhost:8080/api => bundle production nhúng cứng localhost. Ai chạy backend dev thì trình duyệt gửi vân tay vào máy họ thật (biểu hiện: CORS error + preflight 500 trong DevTools). BÀI HỌC CHUNG: getApiBaseUrl() cố ý quyết định lúc CHẠY, nhưng 'vite define' cho phép đi vòng qua nó lúc BUILD - hễ thấy process.env.* dùng làm ĐỊA CHỈ trong mã app-shell thì phải nghi. Cách soi rẻ nhất, không cần trình duyệt: tải bundle production về rồi grep chuỗi localhost. Nay có getFingerprintEndpoint() + test runtime.test.ts khoá lại.
[2026-08-01 17:35] 01/08 agent-2: HỢP NHẤT /login cũ vào trang SSO :3004 (frontend-id). Giữ vanilla theo cam kết ở đầu idClient.ts (nơi DUY NHẤT nhận mật khẩu ⇒ mặt tiếp xúc nhỏ để rà soát bảo mật một lượt): bố cục 2 cột + ảnh nền + khẩu hiệu luân phiên + khối liên hệ dựng bằng HTML/CSS thuần, icon là SVG nội tuyến thay lucide-react. JS chỉ 7,1 KB (3,26 KB nén). BỐN BẪY đã trả giá: (1) CSP ở frontend-id/public/_headers là script-src 'self' nên Turnstile bị chặn hoàn toàn - phải nới script-src + frame-src cho challenges.cloudflare.com; frame-ancestors 'none' GIỮ NGUYÊN vì nó nói về ai được nhúng TA, khác hướng hoàn toàn. (2) Đăng nhập chỉ bằng MFA yêu cầu KHÔNG có trường password trong body, gửi password:'' là rơi lại luồng mật khẩu - idClient.login nay tự lọc bỏ trường rỗng. (3) .hop TUYỆT ĐỐI không được overflow:hidden vì các bó đường nối là con của nó và cố ý nằm ngoài mép; bo góc giao cho .than. (4) Chặn 423 phải GIẤU biểu mẫu chứ không chỉ báo đỏ - ssoController cũng ném ACCOUNT_DISABLED nên lỗi này chạm được cả khi người dùng không gõ mật khẩu. Đã thêm 5 test khoá các nhánh này, tổng 14/14 xanh.
[2026-08-01 17:35] 01/08 agent-2 (mẹo kiểm chứng): muốn chụp ảnh một TRẠNG THÁI TĨNH của trang có JS thì phải đổi <script type="module"> thành type="text/plain"; thêm một thuộc tính lạ (vd data-tat) KHÔNG chặn được, module vẫn chạy và vẽ lại DOM về mặc định - đã mất một vòng chụp vì tưởng CSS hỏng. Chrome dùng lại bản trong frontend-kiosk/chrome/ kèm LD_LIBRARY_PATH tới .tools/libs, không cần tải gì.
[2026-08-01 17:47] 01/08 agent-2: trang :3004 gỡ lưới mạch nền + 11 vệt sáng, CHỈ giữ 4 bó đường nối panel với nền; đổi bảng màu sang xanh dương. BẪY: 4 mask mờ dần của các bó nằm trong <defs> của chính SVG nền - gỡ cả thẻ đó là 4 bó BIẾN MẤT SẠCH (mask không phân giải được thì trình duyệt ẩn toàn bộ phần tử, không phải bỏ qua). Nay giữ thẻ rỗng class 'kho-dinh-nghia' chỉ chứa defs, và phải position:absolute + width/height 0, nếu không nó chiếm một ô trong lưới place-items:center của body và đẩy panel lệch. Thứ làm ảnh trong panel bị che KHÔNG phải box-shadow mà là gradient .cot-anh::after phủ tối 18-32% lên toàn ảnh; nay chỉ còn hoà mép phải từ 88% trở đi. Bảng màu phải giữ đúng BA bậc: nền #071a30 < the-sau #0e2647 < the #123055 - san bằng là panel chìm vào nền (đã phải hạ nền một bậc sau lần chụp đầu).
[2026-08-01 18:05] 01/08 agent-2: trang :3004 chuyển sang NỀN SÁNG dùng đúng --app-bg #f1f5f9 của app-shell (khớp bảng điều khiển sau đăng nhập, tránh nháy đổi tông), panel #e8f0fb, bề mặt phụ trắng; chân cắm đổi từ vàng sang xanh #1d4ed8 và bỏ drop-shadow vì vàng trên nền sáng gần như biến mất. BA điều đo thật chứ không nhìn mắt: (1) overflow:hidden trên body CHẶN người dùng cuộn nhưng KHÔNG chặn cuộn bằng script - đo bằng window.scrollTo rồi đọc scrollY sẽ ra số khác 0 và tưởng hỏng; bằng chứng đúng là chụp ảnh KHÔNG kèm --hide-scrollbars rồi xem có thanh cuộn không. (2) Panel cao cố định 611px bị cắt mất phần TRÊN ở màn hình thấp (đo được panelTop=-38 ở 1280x660, -178 ở 1400x520) mà trang đã tắt cuộn nên phần đó không với tới - phải cho chiều cao là min(33rem, calc(100vh - 9rem)). (3) MỘT biến --canh điều khiển cả height lẫn cột đầu của grid-template-columns thì ô ảnh luôn vuông; ảnh login-background vốn vuông 1600x1600 nên background-size:contain hiện đủ 100% ảnh, không cắt và không thừa dải trống. Đo lại: ảnh 528x528 y hệt nhau giữa lối mật khẩu và lối MFA ở mọi khung nhìn.
[2026-08-01 18:24] 01/08 agent-2: :3004 gỡ dòng 'Dành cho tài khoản đã bật xác thực 2 lớp' (cao ~48px, đủ làm cột biểu mẫu lối MFA vượt chiều cao panel rồi mọc thanh cuộn riêng - thông tin không mất vì login.ts đã hiện đúng câu đó trong băng thông báo khi backend trả MFA_REQUIRED); nút Mật khẩu/Mã MFA/Đăng nhập và vòng lấy nét chuyển sang xanh #1d4ed8, ĐỎ nay CHỈ dành cho trạng thái lỗi (.loi/.chan) - để vòng đỏ quanh ô đang gõ cạnh nút xanh thì người dùng đọc thành 'ô này đang sai'. BẪY ĐO ĐẠC quan trọng: cờ --window-size của Chrome headless BAO GỒM khung trình duyệt, nên '1366x768' chỉ cho khung nhìn 681px và '1600x1000' cho 913px - đừng suy ra bố cục từ con số truyền vào, phải đọc window.innerHeight. Chính vì hiểu nhầm chỗ này mà tôi tưởng CSS tính sai trong khi nó đúng. Tách --cao (chiều cao khối) khỏi --canh (cạnh ô ảnh vuông): trước đó gộp một, muốn cho biểu mẫu thêm chỗ thì phải nới ô ảnh rộng ra, mà nới rộng thì cột biểu mẫu lại hẹp đi - quẩn. Kết quả đo: 0 tràn ở CẢ 4 trạng thái (mật khẩu / MFA / kèm băng lỗi) tại khung nhìn 993, 913, 813, 681, 633px.
[2026-08-01 18:33] agent-1 · 01/08 18:30 LỖI SSO THỨ HAI (PR #46, merge 14ffebe): 'Phiên đăng nhập không khớp' hiện ra dù đăng nhập ĐÃ THÀNH CÔNG - dấu hiệu nhận biết là bấm 'Đăng nhập lại' thì vào thẳng trang chính, tức cookie đã có từ trước. KHÁC HẲN lỗi state mismatch hôm 31/07 dù cùng một câu thông báo. Gốc: completeSso tiêu thụ sso_state (xoá TRƯỚC khi gọi mạng, có chủ đích) nên KHÔNG idempotent, mà React.StrictMode ở dev chạy effect HAI lần - lần một đổi mã xong nhưng cờ 'cancelled' của cleanup chặn mất bước chuyển hướng, lần hai thấy state rỗng nên báo lỗi. Vá: chốt bằng useRef (ref sống qua vòng unmount/remount giả lập của StrictMode vì cùng fiber; biến khai trong effect thì dựng lại mỗi lần nên vô dụng) + bỏ cờ cancelled vì mã đã đổi và cookie đã đặt thì BẮT BUỘC phải chuyển hướng. QUY TẮC RÚT RA, áp cho mọi effect chạy một lần: hàm có TÁC DỤNG PHỤ TIÊU THỤ (dùng xong là mất: đổi mã, tiêu token, trừ lượt) thì KHÔNG được để StrictMode gọi hai lần, và KHÔNG được dùng cờ cancelled để bỏ qua phần xử lý HẬU tác dụng phụ - tác dụng phụ đã xảy ra rồi, bỏ qua chỉ làm mất kết quả. Test dựng <StrictMode> THẬT + mock theo ngữ nghĩa một-lần mới tái hiện được; mock trả 'ok' mãi thì không bắt được lỗi.
[2026-08-01 18:57] 01/08 agent-2 (:3004 vòng 3): tiêu đề UBND + 'Đăng nhập hệ thống' sang xanh; nới đệm ở màn hình hẹp; khung khẩu hiệu KHÔNG BAO GIỜ biến dạng. Giải pháp cho khẩu hiệu KHÔNG phải ấn định height mà là xếp CẢ BỐN câu chồng nhau ở cùng ô lưới 'grid-area: 1/1' rồi chỉ đổi opacity - khung tự cao bằng câu dài nhất, thêm/sửa câu về sau không phải đo lại, và vì textContent không đổi nên bố cục không hề tính lại. Kèm theo: JS phải tự quản aria-hidden cho ba câu đang trong suốt, vì opacity:0 KHÔNG che khỏi trình đọc màn hình - để nguyên là người dùng nghe liền cả bốn câu. Khẩu hiệu chuyển ra NGOÀI panel thành hàng 1 của lưới body (auto auto, align-content:center trên máy tính nên vẫn sát panel; auto 1fr ở màn hình hẹp nên neo đỉnh còn panel canh giữa) và phải z-index:3 vì bó đường nối phía trên vắt qua đúng chỗ đó. BẪY ĐÃ TRẢ GIÁ: công thức chiều cao panel tách riêng hằng số 2.5rem cho đệm body, trong khi media query màn hình thấp lại hạ đệm xuống 0.5rem => trừ dư 24px, lối MFA kèm băng lỗi tràn. Gom TẤT CẢ vào một biến '--tru' duy nhất thì không còn hai nguồn sự thật để lệch. Đo lại: 0 tràn ở cả 3 trạng thái tại khung nhìn 993/813/681/633px; khung khẩu hiệu cao y hệt ở cả 4 câu và ở mọi bề rộng 1000-1920px.
